# primary role is: Controller
heat_template_version: rocky

description: >
  Deploy an OpenStack environment, consisting of several node types (roles),
  Controller, Compute, BlockStorage, SwiftStorage and CephStorage. The Storage
  roles enable independent scaling of the storage components, but the minimal
  deployment is one Controller and one Compute node.


# TODO(shadower): we should probably use the parameter groups to put
# some order in here.
parameters:

  # Common parameters (not specific to
  # Special case the External hostname param, which is CloudName
  CloudName:
    default: overcloud.localdomain
    description: The DNS name of this cloud. E.g. ci-overcloud.tripleo.org
    type: string
  # TODO (dsneddon) Legacy name, eventually refactor to match network name
  PublicVirtualFixedIPs:
    default: []
    description: >
        Control the IP allocation for the PublicVirtualInterface port. E.g.
        [{'ip_address':'1.2.3.4'}]
    type: json
  ExternalVirtualFixedIPs:
    default: []
    description: >
        Control the IP allocation for the ExternalVirtualInterface port. E.g.
        [{'ip_address':'1.2.3.4'}]
    type: json
  # Special case the Internal API hostname param, which is CloudNameInternal
  CloudNameInternal:
    default: overcloud.internalapi.localdomain
    description: >
      The DNS name of this cloud's internal_api endpoint. E.g.
      'ci-overcloud.internalapi.tripleo.org'.
    type: string
  InternalApiVirtualFixedIPs:
    default: []
    description: >
        Control the IP allocation for the InternalApiVirtualInterface port. E.g.
        [{'ip_address':'1.2.3.4'}]
    type: json
  CloudNameStorage:
    default: overcloud.storage.localdomain
    description: >
      The DNS name of this cloud's storage endpoint. E.g.
      'ci-overcloud.storage.tripleo.org'.
    type: string
  StorageVirtualFixedIPs:
    default: []
    description: >
        Control the IP allocation for the StorageVirtualInterface port. E.g.
        [{'ip_address':'1.2.3.4'}]
    type: json
  # Special case StorageMgmt hostname param, which is CloudNameStorageManagement
  CloudNameStorageManagement:
    default: overcloud.storagemgmt.localdomain
    description: >
      The DNS name of this cloud's storage_mgmt endpoint. E.g.
      'ci-overcloud.storagemgmt.tripleo.org'.
    type: string
  StorageMgmtVirtualFixedIPs:
    default: []
    description: >
        Control the IP allocation for the StorageMgmtVirtualInterface port. E.g.
        [{'ip_address':'1.2.3.4'}]
    type: json
  CloudNameCtlplane:
    default: overcloud.ctlplane.localdomain
    description: >
      The DNS name of this cloud's provisioning network endpoint. E.g.
      'ci-overcloud.ctlplane.tripleo.org'.
    type: string
  ExtraHostFileEntries:
    default: []
    description: List of extra hosts entries to be appended to /etc/hosts
    type: comma_delimited_list
  UndercloudHostsEntries:
    default: []
    description: >
      List of undercloud hosts entries to be appended to /etc/hosts. The
      value is populated with the HEAT_HOSTS entries on the undercloud by
      tripleoclient when running deploy.
    type: comma_delimited_list
  EndpointMapOverride:
    default: {}
    description: Can be used to override the calcluated EndpointMap
    type: json
  ExtraConfig:
    default: {}
    description: |
      Additional hiera configuration to inject into the cluster.
    type: json
  NeutronControlPlaneID:
    default: 'ctlplane'
    type: string
    description: Neutron ID or name for ctlplane network.
  NeutronPhysicalBridge:
    default: 'br-ex'
    description: An OVS bridge to create for accessing external networks.
    type: string
  NeutronPublicInterface:
    default: nic1
    description: Which interface to add to the NeutronPhysicalBridge.
    type: string
  ControlPlaneSubnet:
    description: The name of the undercloud Neutron control plane subnet
    default: ctlplane-subnet
    type: string
  ControlPlaneSubnetCidr:
    default: ''
    description: >
      The subnet CIDR of the control plane network. (The parameter is
      automatically resolved from the ctlplane subnet's cidr attribute.)
    type: string
  ControlFixedIPs:
    default: []
    description: >
        Control the IP allocation for the ControlVirtualIP port. E.g.
        [{'ip_address':'1.2.3.4'}]
    type: json
  RabbitCookieSalt:
    type: string
    default: unset
    description: Salt for the rabbit cookie, change this to force the randomly generated rabbit cookie to change.
  RedisVirtualFixedIPs:
    default: []
    description: >
        Control the IP allocation for the virtual IP used by Redis. E.g.
        [{'ip_address':'1.2.3.4'}]
    type: json
  OVNDBsVirtualFixedIPs:
    default: []
    description: >
        Control the IP allocation for the virtual IP used by OVN DBs. E.g.
        [{'ip_address':'1.2.3.4'}]
    type: json
  CloudDomain:
    default: 'localdomain'
    type: string
    description: >
      The DNS domain used for the hosts. This must match the
      overcloud_domain_name configured on the undercloud.
  ServerMetadata:
    default: {}
    description: >
      Extra properties or metadata passed to Nova for the created nodes in
      the overcloud. It's accessible via the Nova metadata API.
    type: json
  NetworkDeploymentActions:
    type: comma_delimited_list
    description: >
      Heat action when to apply network configuration changes
    default: ['CREATE']
# Compute-specific params
# FIXME(shardy) handle these deprecated names as they don't match compute.yaml
  HypervisorNeutronPhysicalBridge:
    default: 'br-ex'
    description: >
      An OVS bridge to create on each hypervisor. This defaults to br-ex the
      same as the control plane nodes, as we have a uniform configuration of
      the openvswitch agent. Typically should not need to be changed.
    type: string
  HypervisorNeutronPublicInterface:
    default: nic1
    description: What interface to add to the HypervisorNeutronPhysicalBridge.
    type: string

  NodeCreateBatchSize:
    default: 30
    description: Maxiumum batch size for creating nodes
    type: number

  NovaAdditionalCell:
    default: false
    description: Whether this is an cell additional to the default cell.
    type: boolean

  NovaLocalMetadataPerCell:
    default: false
    description: >
      Indicates that the nova-metadata API service has been deployed
      per-cell, so that we can have better performance and data isolation in a
      multi-cell deployment. Users should consider the use of this configuration
      depending on how neutron is setup. If networks span cells, you might need
      to run nova-metadata API service globally. If your networks are segmented
      along cell boundaries, then you can run nova-metadata API service per cell.
      When running nova-metadata API service per cell, you should also configure
      each Neutron metadata-agent to point to the corresponding nova-metadata API
      service.
    type: boolean

  # Jinja loop for Role in role_data.yaml

  ControllerExtraConfig:
    default: {}
    description: |
      Role specific additional hiera configuration to inject into the cluster.
    type: json
  controllerExtraConfig:
    default: {}
    description: |
      DEPRECATED use ControllerExtraConfig instead
    type: json
  # Parameters generated for Controller Role
  ControllerServices:
    description: A list of service resources (configured in the Heat
                 resource_registry) which represent nested stacks
                 for each service that should get installed on the Controller role.
    type: comma_delimited_list
  ControllerNetworkDeploymentActions:
    type: comma_delimited_list
    description: >
      Heat action when to apply network configuration changes
    default: []
  ControllerAnyErrorsFatal:
    default: yes
    type: string
  ControllerMaxFailPercentage:
    default: 0
    type: number
  ControllerCount:
    description: Number of Controller nodes to deploy
    type: number
    default: 1

  ControllerHostnameFormat:
    type: string
    description: >
      Format for Controller node hostnames
      Note %index% is translated into the index of the node, e.g 0/1/2 etc
      and %stackname% is replaced with the stack name e.g overcloud
  
    default: "%stackname%-controller-%index%"
  
  ControllerRemovalPolicies:
    default: []
    type: json
    description: >
      List of resources to be removed from Controller ResourceGroup when
      doing an update which requires removal of specific resources.
      Example format ComputeRemovalPolicies: [{'resource_list': ['0']}]

  ControllerRemovalPoliciesMode:
    default: append
    type: string
    description: >
      How to handle change to RemovalPolicies for Controller
      ResourceGroup when doing an update. Default mode 'append' will
      append to the existing blacklist and 'update' would replace
      the blacklist.

  ControllerSchedulerHints:
    type: json
    description: Optional scheduler hints to pass to nova
    default: {}

  ControllerParameters:
    type: json
    description: Optional Role Specific parameters to be provided to service
    default: {}

  ControllerExtraGroupVars:
    type: json
    description: Optional extra Ansible group vars
    default: {}

  ControllerServiceNetMap:
    default: {}
    description: |
      Role specific ServiceNetMap overrides, the map provided will be merged
      with the global ServiceNetMap when passing the ServiceNetMap to the
      ControllerServiceChain resource and the Controller resource group.
      For example:
      ControllerServiceNetMap:
        NovaLibvirtNetwork: internal_api_leaf2
    type: json

  ComputeExtraConfig:
    default: {}
    description: |
      Role specific additional hiera configuration to inject into the cluster.
    type: json
  NovaComputeExtraConfig:
    default: {}
    description: |
      DEPRECATED use ComputeExtraConfig instead
    type: json
  # Parameters generated for Compute Role
  ComputeServices:
    description: A list of service resources (configured in the Heat
                 resource_registry) which represent nested stacks
                 for each service that should get installed on the Compute role.
    type: comma_delimited_list
  ComputeNetworkDeploymentActions:
    type: comma_delimited_list
    description: >
      Heat action when to apply network configuration changes
    default: []
  ComputeAnyErrorsFatal:
    default: yes
    type: string
  ComputeMaxFailPercentage:
    default: 0
    type: number
  ComputeCount:
    description: Number of Compute nodes to deploy
    type: number
    default: 1

  ComputeHostnameFormat:
    type: string
    description: >
      Format for Compute node hostnames
      Note %index% is translated into the index of the node, e.g 0/1/2 etc
      and %stackname% is replaced with the stack name e.g overcloud
  
    default: "%stackname%-compute-%index%"
  
  ComputeRemovalPolicies:
    default: []
    type: json
    description: >
      List of resources to be removed from Compute ResourceGroup when
      doing an update which requires removal of specific resources.
      Example format ComputeRemovalPolicies: [{'resource_list': ['0']}]

  ComputeRemovalPoliciesMode:
    default: append
    type: string
    description: >
      How to handle change to RemovalPolicies for Compute
      ResourceGroup when doing an update. Default mode 'append' will
      append to the existing blacklist and 'update' would replace
      the blacklist.

  ComputeSchedulerHints:
    type: json
    description: Optional scheduler hints to pass to nova
    default: {}
  NovaComputeSchedulerHints:
    type: json
    description: DEPRECATED - use ComputeSchedulerHints instead
    default: {}

  ComputeParameters:
    type: json
    description: Optional Role Specific parameters to be provided to service
    default: {}

  ComputeExtraGroupVars:
    type: json
    description: Optional extra Ansible group vars
    default: {}

  ComputeServiceNetMap:
    default: {}
    description: |
      Role specific ServiceNetMap overrides, the map provided will be merged
      with the global ServiceNetMap when passing the ServiceNetMap to the
      ComputeServiceChain resource and the Compute resource group.
      For example:
      ComputeServiceNetMap:
        NovaLibvirtNetwork: internal_api_leaf2
    type: json

  ContrailControllerExtraConfig:
    default: {}
    description: |
      Role specific additional hiera configuration to inject into the cluster.
    type: json
  # Parameters generated for ContrailController Role
  ContrailControllerServices:
    description: A list of service resources (configured in the Heat
                 resource_registry) which represent nested stacks
                 for each service that should get installed on the ContrailController role.
    type: comma_delimited_list
  ContrailControllerNetworkDeploymentActions:
    type: comma_delimited_list
    description: >
      Heat action when to apply network configuration changes
    default: []
  ContrailControllerAnyErrorsFatal:
    default: yes
    type: string
  ContrailControllerMaxFailPercentage:
    default: 0
    type: number
  ContrailControllerCount:
    description: Number of ContrailController nodes to deploy
    type: number
    default: 1

  ContrailControllerHostnameFormat:
    type: string
    description: >
      Format for ContrailController node hostnames
      Note %index% is translated into the index of the node, e.g 0/1/2 etc
      and %stackname% is replaced with the stack name e.g overcloud
  
    default: "%stackname%-contrailcontroller-%index%"
  
  ContrailControllerRemovalPolicies:
    default: []
    type: json
    description: >
      List of resources to be removed from ContrailController ResourceGroup when
      doing an update which requires removal of specific resources.
      Example format ComputeRemovalPolicies: [{'resource_list': ['0']}]

  ContrailControllerRemovalPoliciesMode:
    default: append
    type: string
    description: >
      How to handle change to RemovalPolicies for ContrailController
      ResourceGroup when doing an update. Default mode 'append' will
      append to the existing blacklist and 'update' would replace
      the blacklist.

  ContrailControllerSchedulerHints:
    type: json
    description: Optional scheduler hints to pass to nova
    default: {}

  ContrailControllerParameters:
    type: json
    description: Optional Role Specific parameters to be provided to service
    default: {}

  ContrailControllerExtraGroupVars:
    type: json
    description: Optional extra Ansible group vars
    default: {}

  ContrailControllerServiceNetMap:
    default: {}
    description: |
      Role specific ServiceNetMap overrides, the map provided will be merged
      with the global ServiceNetMap when passing the ServiceNetMap to the
      ContrailControllerServiceChain resource and the ContrailController resource group.
      For example:
      ContrailControllerServiceNetMap:
        NovaLibvirtNetwork: internal_api_leaf2
    type: json

  ContrailSriovExtraConfig:
    default: {}
    description: |
      Role specific additional hiera configuration to inject into the cluster.
    type: json
  # Parameters generated for ContrailSriov Role
  ContrailSriovServices:
    description: A list of service resources (configured in the Heat
                 resource_registry) which represent nested stacks
                 for each service that should get installed on the ContrailSriov role.
    type: comma_delimited_list
  ContrailSriovNetworkDeploymentActions:
    type: comma_delimited_list
    description: >
      Heat action when to apply network configuration changes
    default: []
  ContrailSriovAnyErrorsFatal:
    default: yes
    type: string
  ContrailSriovMaxFailPercentage:
    default: 0
    type: number
  ContrailSriovCount:
    description: Number of ContrailSriov nodes to deploy
    type: number
    default: 0

  ContrailSriovHostnameFormat:
    type: string
    description: >
      Format for ContrailSriov node hostnames
      Note %index% is translated into the index of the node, e.g 0/1/2 etc
      and %stackname% is replaced with the stack name e.g overcloud
  
    default: "%stackname%-contrailsriov-%index%"
  
  ContrailSriovRemovalPolicies:
    default: []
    type: json
    description: >
      List of resources to be removed from ContrailSriov ResourceGroup when
      doing an update which requires removal of specific resources.
      Example format ComputeRemovalPolicies: [{'resource_list': ['0']}]

  ContrailSriovRemovalPoliciesMode:
    default: append
    type: string
    description: >
      How to handle change to RemovalPolicies for ContrailSriov
      ResourceGroup when doing an update. Default mode 'append' will
      append to the existing blacklist and 'update' would replace
      the blacklist.

  ContrailSriovSchedulerHints:
    type: json
    description: Optional scheduler hints to pass to nova
    default: {}

  ContrailSriovParameters:
    type: json
    description: Optional Role Specific parameters to be provided to service
    default: {}

  ContrailSriovExtraGroupVars:
    type: json
    description: Optional extra Ansible group vars
    default: {}

  ContrailSriovServiceNetMap:
    default: {}
    description: |
      Role specific ServiceNetMap overrides, the map provided will be merged
      with the global ServiceNetMap when passing the ServiceNetMap to the
      ContrailSriovServiceChain resource and the ContrailSriov resource group.
      For example:
      ContrailSriovServiceNetMap:
        NovaLibvirtNetwork: internal_api_leaf2
    type: json

  ContrailDpdkExtraConfig:
    default: {}
    description: |
      Role specific additional hiera configuration to inject into the cluster.
    type: json
  # Parameters generated for ContrailDpdk Role
  ContrailDpdkServices:
    description: A list of service resources (configured in the Heat
                 resource_registry) which represent nested stacks
                 for each service that should get installed on the ContrailDpdk role.
    type: comma_delimited_list
  ContrailDpdkNetworkDeploymentActions:
    type: comma_delimited_list
    description: >
      Heat action when to apply network configuration changes
    default: []
  ContrailDpdkAnyErrorsFatal:
    default: yes
    type: string
  ContrailDpdkMaxFailPercentage:
    default: 0
    type: number
  ContrailDpdkCount:
    description: Number of ContrailDpdk nodes to deploy
    type: number
    default: 0

  ContrailDpdkHostnameFormat:
    type: string
    description: >
      Format for ContrailDpdk node hostnames
      Note %index% is translated into the index of the node, e.g 0/1/2 etc
      and %stackname% is replaced with the stack name e.g overcloud
  
    default: "%stackname%-contrailsriov-%index%"
  
  ContrailDpdkRemovalPolicies:
    default: []
    type: json
    description: >
      List of resources to be removed from ContrailDpdk ResourceGroup when
      doing an update which requires removal of specific resources.
      Example format ComputeRemovalPolicies: [{'resource_list': ['0']}]

  ContrailDpdkRemovalPoliciesMode:
    default: append
    type: string
    description: >
      How to handle change to RemovalPolicies for ContrailDpdk
      ResourceGroup when doing an update. Default mode 'append' will
      append to the existing blacklist and 'update' would replace
      the blacklist.

  ContrailDpdkSchedulerHints:
    type: json
    description: Optional scheduler hints to pass to nova
    default: {}

  ContrailDpdkParameters:
    type: json
    description: Optional Role Specific parameters to be provided to service
    default: {}

  ContrailDpdkExtraGroupVars:
    type: json
    description: Optional extra Ansible group vars
    default: {}

  ContrailDpdkServiceNetMap:
    default: {}
    description: |
      Role specific ServiceNetMap overrides, the map provided will be merged
      with the global ServiceNetMap when passing the ServiceNetMap to the
      ContrailDpdkServiceChain resource and the ContrailDpdk resource group.
      For example:
      ContrailDpdkServiceNetMap:
        NovaLibvirtNetwork: internal_api_leaf2
    type: json

  ComputeNFVExtraConfig:
    default: {}
    description: |
      Role specific additional hiera configuration to inject into the cluster.
    type: json
  # Parameters generated for ComputeNFV Role
  ComputeNFVServices:
    description: A list of service resources (configured in the Heat
                 resource_registry) which represent nested stacks
                 for each service that should get installed on the ComputeNFV role.
    type: comma_delimited_list
  ComputeNFVNetworkDeploymentActions:
    type: comma_delimited_list
    description: >
      Heat action when to apply network configuration changes
    default: []
  ComputeNFVAnyErrorsFatal:
    default: yes
    type: string
  ComputeNFVMaxFailPercentage:
    default: 0
    type: number
  ComputeNFVCount:
    description: Number of ComputeNFV nodes to deploy
    type: number
    default: 0

  ComputeNFVHostnameFormat:
    type: string
    description: >
      Format for ComputeNFV node hostnames
      Note %index% is translated into the index of the node, e.g 0/1/2 etc
      and %stackname% is replaced with the stack name e.g overcloud
  
    default: "%stackname%-compute-%index%"
  
  ComputeNFVRemovalPolicies:
    default: []
    type: json
    description: >
      List of resources to be removed from ComputeNFV ResourceGroup when
      doing an update which requires removal of specific resources.
      Example format ComputeRemovalPolicies: [{'resource_list': ['0']}]

  ComputeNFVRemovalPoliciesMode:
    default: append
    type: string
    description: >
      How to handle change to RemovalPolicies for ComputeNFV
      ResourceGroup when doing an update. Default mode 'append' will
      append to the existing blacklist and 'update' would replace
      the blacklist.

  ComputeNFVSchedulerHints:
    type: json
    description: Optional scheduler hints to pass to nova
    default: {}

  ComputeNFVParameters:
    type: json
    description: Optional Role Specific parameters to be provided to service
    default: {}

  ComputeNFVExtraGroupVars:
    type: json
    description: Optional extra Ansible group vars
    default: {}

  ComputeNFVServiceNetMap:
    default: {}
    description: |
      Role specific ServiceNetMap overrides, the map provided will be merged
      with the global ServiceNetMap when passing the ServiceNetMap to the
      ComputeNFVServiceChain resource and the ComputeNFV resource group.
      For example:
      ComputeNFVServiceNetMap:
        NovaLibvirtNetwork: internal_api_leaf2
    type: json


  # Identifiers to trigger tasks on nodes
  UpdateIdentifier:
    default: ''
    type: string
    description: >
      Setting to a previously unused value during stack-update will trigger
      package update on all nodes
  DeployIdentifier:
    default: ''
    type: string
    description: >
      Setting this to a unique value will re-run any deployment tasks which
      perform configuration on a Heat stack-update.
  AddVipsToEtcHosts:
    default: True
    type: boolean
    description: >
      Set to true to append per network Vips to /etc/hosts on each node.

  DeploymentServerBlacklist:
    default: []
    type: comma_delimited_list
    description: >
      List of server hostnames to blacklist from any triggered deployments.

  GlobalConfigExtraMapData:
    type: json
    default: {}
    description: Map of extra global_config_settings data to set on each node.

  RootStackName:
    description: The name of the stack/plan.
    type: string


parameter_groups:
- label: deprecated
  description: Do not use deprecated params, they will be removed.
  parameters:
    - controllerExtraConfig
parameter_groups:
- label: deprecated
  description: Do not use deprecated params, they will be removed.
  parameters:
    - NovaComputeSchedulerHints
    - NovaComputeExtraConfig

conditions:
  add_vips_to_etc_hosts: {equals : [{get_param: AddVipsToEtcHosts}, True]}
  control_fixed_ip_not_set: {equals : [{get_param: ControlFixedIPs}, []]}
  ctlplane_subnet_cidr_set:
    not:
      equals: [{get_param: ControlPlaneSubnetCidr}, '']
  internal_api_virtual_fixed_ip_set:
    not:
      equals:
        - get_param: InternalApiVirtualFixedIPs
        - []
  storage_virtual_fixed_ip_set:
    not:
      equals:
        - get_param: StorageVirtualFixedIPs
        - []
  storage_mgmt_virtual_fixed_ip_set:
    not:
      equals:
        - get_param: StorageMgmtVirtualFixedIPs
        - []
  tenant_virtual_fixed_ip_set:
    not:
      equals:
        - get_param: TenantVirtualFixedIPs
        - []
  public_virtual_fixed_ip_set:
    not:
      equals:
        - get_param: PublicVirtualFixedIPs
        - []
  redis_virtual_fixed_ip_set:
    not:
      equals:
        - get_param: RedisVirtualFixedIPs
        - []
  ovn_dbs_virtual_fixed_ip_set:
    not:
      equals:
        - get_param: OVNDBsVirtualFixedIPs
        - []
  set_default_mysql_cell_internal:
    or:
      - equals:
        - get_param: NovaAdditionalCell
        - true
      - and:
        - equals:
          - get_param: NovaAdditionalCell
          - false
        - equals:
          - get_param: [EndpointMapOverride, MysqlCellInternal]
          - ''
  Controller_network_deployment_actions_exists:
    not:
      equals:
        - {get_param: ControllerNetworkDeploymentActions}
        - []
  Compute_network_deployment_actions_exists:
    not:
      equals:
        - {get_param: ComputeNetworkDeploymentActions}
        - []
  ContrailController_network_deployment_actions_exists:
    not:
      equals:
        - {get_param: ContrailControllerNetworkDeploymentActions}
        - []
  ContrailSriov_network_deployment_actions_exists:
    not:
      equals:
        - {get_param: ContrailSriovNetworkDeploymentActions}
        - []
  ContrailDpdk_network_deployment_actions_exists:
    not:
      equals:
        - {get_param: ContrailDpdkNetworkDeploymentActions}
        - []
  ComputeNFV_network_deployment_actions_exists:
    not:
      equals:
        - {get_param: ComputeNFVNetworkDeploymentActions}
        - []
  set_default_nova_vnc_proxy_cell_public:
    or:
      - equals:
        - get_param: NovaAdditionalCell
        - true
      - and:
        - equals:
          - get_param: NovaAdditionalCell
          - false
        - equals:
          - get_param: [EndpointMapOverride, NovaVNCProxyCellPublic]
          - ''
  set_default_nova_metadata_cell_internal:
    or:
      - equals:
        - get_param: NovaLocalMetadataPerCell
        - true
      - and:
        - equals:
          - get_param: NovaLocalMetadataPerCell
          - false
        - equals:
          - get_param: [EndpointMapOverride, NovaMetadataCellInternal]
          - ''

resources:

  VipHosts:
    type: OS::Heat::Value
    properties:
      type: comma_delimited_list
      value:
        - str_replace:
            template: IP  HOST
            params:
              IP: {get_attr: [VipMap, net_ip_map, ctlplane]}
              HOST: {get_param: CloudNameCtlplane}
  # Special case the External hostname param, which is CloudName
        - str_replace:
            template: IP  HOST
            params:
              IP: {get_attr: [VipMap, net_ip_map, external]}
              HOST: {get_param: CloudName}
  # Special case the Internal API hostname param, which is CloudNameInternal
        - str_replace:
            template: IP  HOST
            params:
              IP: {get_attr: [VipMap, net_ip_map, internal_api]}
              HOST: {get_param: CloudNameInternal}
        - str_replace:
            template: IP  HOST
            params:
              IP: {get_attr: [VipMap, net_ip_map, storage]}
              HOST: {get_param: CloudNameStorage}
  # Special case StorageMgmt hostname param, which is CloudNameStorageManagement
        - str_replace:
            template: IP  HOST
            params:
              IP: {get_attr: [VipMap, net_ip_map, storage_mgmt]}
              HOST: {get_param: CloudNameStorageManagement}

  HeatAuthEncryptionKey:
    type: OS::TripleO::RandomString

  PcsdPassword:
    type: OS::TripleO::RandomString
    properties:
      length: 16

  HorizonSecret:
    type: OS::TripleO::RandomString
    properties:
      length: 64

  NetCidrMapValue:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_replace:
        - map_merge:
          - {get_attr: [Networks, net_cidr_map]}
          # NOTE(hjensas): When ctlplane network and subnets are created by the
          # undercloud installer, the subnet cidrs are added as tags.
          - ctlplane: {get_attr: [ControlVirtualIP, network, tags]}
        - keys:
            ctlplane: {get_param: NeutronControlPlaneID}

  ServiceNetMap:
    type: OS::TripleO::ServiceNetMap

  EndpointMap:
    type: OS::TripleO::EndpointMap
    properties:
      CloudEndpoints:
        ctlplane: {get_param: CloudNameCtlplane}
  # Special case the External hostname param, which is CloudName
        external: {get_param: CloudName}
  # Special case the Internal API hostname param, which is CloudNameInternal
        internal_api: {get_param: CloudNameInternal}
        storage: {get_param: CloudNameStorage}
  # Special case StorageMgmt hostname param, which is CloudNameStorageManagement
        storage_mgmt: {get_param: CloudNameStorageManagement}
      NetIpMap: {get_attr: [VipMap, net_ip_map]}
      ServiceNetMap: {get_attr: [ServiceNetMap, service_net_map]}

  EndpointMapData:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:
          - {get_attr: [EndpointMap, endpoint_map]}
          - {get_param: EndpointMapOverride}
            # For parent stack we must set these to the local endpoints
            # For split-controlplane stacks that are nova cells we must set
            # these to the local endpoints
            # For split-controlplane stacks that are not nova cells we should
            # take these from EndpointMapOverride (i.e the parent stack)
          - if:
            - set_default_mysql_cell_internal
            - MysqlCellInternal: {get_attr: [EndpointMap, endpoint_map, MysqlInternal]}
            - {}
          - if:
            - set_default_nova_vnc_proxy_cell_public
            - NovaVNCProxyCellPublic: {get_attr: [EndpointMap, endpoint_map, NovaVNCProxyPublic]}
            - {}
          - if:
            - set_default_nova_metadata_cell_internal
            - NovaMetadataCellInternal: {get_attr: [EndpointMap, endpoint_map, NovaMetadataInternal]}
            - {}

  # Creates the "heat-admin" user if configured via the environment
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  NodeAdminUserData:
    type: OS::TripleO::NodeAdminUserData

  # Bootstraps an ntp configuration and includes a hardware clock sync to
  # for containers.
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  NodeTimesyncUserData:
    type: OS::TripleO::NodeTimesyncUserData

  # For optional operator additional userdata
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  NodeUserData:
    type: OS::TripleO::NodeUserData

  # Jinja loop for Role in roles_data.yaml

  # Resources generated for Controller Role
  ControllerServiceChain:
    type: OS::TripleO::ControllerServices
    properties:
      Services:
        get_param: ControllerServices
      ServiceNetMap:
        map_merge:
          - {get_attr: [ServiceNetMap, service_net_map]}
          - {get_param: ControllerServiceNetMap}
      ServiceData:
        net_cidr_map: {get_attr: [NetCidrMapValue, value]}
        net_vip_map: {get_attr: [VipMap, net_ip_map]}
      EndpointMap: {get_attr: [EndpointMapData, value]}
      DefaultPasswords: {get_attr: [DefaultPasswords, passwords]}
      RoleName: Controller
      RoleParameters:
        map_merge:
          - {}
          - get_param: ControllerParameters

  # Lookup of role_data via heat outputs is slow, so workaround this by caching
  # the value in an OS::Heat::Value resource
  ControllerServiceChainRoleData:
    type: OS::Heat::Value
    properties:
      type: json
      value: {get_attr: [ControllerServiceChain, role_data]}

  ControllerNetworkDeploymentActionsValue:
    type: OS::Heat::Value
    properties:
      value:
        if:
        - Controller_network_deployment_actions_exists
        - {get_param: ControllerNetworkDeploymentActions}
        - {get_param: NetworkDeploymentActions}

  ControllerConfigData:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        service_configs: {get_attr: [ControllerServiceConfigSettings, value]}
        service_names: {get_attr: [ControllerServiceNames, value]}
        role_extraconfig:
          map_merge:
            - tripleo::profile::base::metrics::collectd::sensubility::subscriptions: {get_attr: [ControllerServiceChainRoleData, value, monitoring_subscriptions]}
            - {get_param: controllerExtraConfig}
            - {get_param: ControllerExtraConfig}
        extraconfig: {get_param: ExtraConfig}
        hieradata_files:
          - '%{::uuid}'
          - fqdn
          - docker_puppet # Optionally provided by container-puppet.py
          - ansible_managed
          - heat_config_%{::deploy_config_name}
          - config_step
          - role_extraconfig
          - extraconfig
          - pci_passthrough_whitelist
          - service_names
          - service_configs
          - cloud_domain
          - bootstrap_node # provided by tripleo-hieradata
          - all_nodes # provided by tripleo-hieradata
          - vip_data # provided by tripleo-hieradata
          - net_ip_map
          - '%{::osfamily}'
          # The following are required for compatibility with the Controller role
          # where some vendor integrations added hieradata via ExtraConfigPre
          - neutron_bigswitch_data # Optionally provided by Controller/ComputeExtraConfigPre
          # Special variable for upgrade
          - upgrade

  ControllerServiceConfigSettings:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:
          - get_param: GlobalConfigExtraMapData
          - get_attr: [ControllerServiceChainRoleData, value, config_settings]
          
          - get_attr: [ControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailSriovServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailDpdkServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeNFVServiceChainRoleData, value, global_config_settings]
          
          # This next step combines two yaql passes:
          # - The inner one does a deep merge on the service_config_settings for all roles
          # - The outer one filters the map based on the services enabled for the role
          #   then merges the result into one map.
          - yaql:
              expression: let(root => $) -> $.data.map.items().where($[0] in coalesce($root.data.services, [])).select($[1]).reduce($1.mergeWith($2), {})
              data:
                map:
                  yaql:
                    expression: $.data.where($ != null).reduce($1.mergeWith($2), {})
                    data:
                    
                      - get_attr: [ControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailSriovServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailDpdkServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeNFVServiceChainRoleData, value, service_config_settings]
                    
                services: {get_attr: [ControllerServiceNames, value]}

  # Filter any null/None service_names which may be present due to mapping
  # of services to OS::Heat::None
  ControllerServiceNames:
    type: OS::Heat::Value
    depends_on: ControllerServiceChain
    properties:
      type: comma_delimited_list
      value:
        yaql:
          expression: let(root => $) -> distinct($.data.extra_services.items().where($[0] in coalesce($root.data.enabled_services, [])).select($[1]).flatten() + coalesce($root.data.enabled_services, []))
          data:
            enabled_services: {get_attr: [ControllerServiceChainRoleData, value, service_names]}
            extra_services:
              # If anything other than keystone needs this
              # then we should add an extra_networks interface
              # to the service templates role_data but for
              # now we hard-code the keystone special case
              keystone:
                - keystone_admin_api
                - keystone_public_api

  ControllerIpListMap:
    type: OS::TripleO::Network::Ports::NetIpListMap
    properties:
      ControlPlaneIpList: {get_attr: [Controller, ip_address]}
      ExternalIpList: {get_attr: [Controller, external_ip_address]}
      InternalApiIpList: {get_attr: [Controller, internal_api_ip_address]}
      StorageIpList: {get_attr: [Controller, storage_ip_address]}
      StorageMgmtIpList: {get_attr: [Controller, storage_mgmt_ip_address]}
      RoleNetworks:
        - ctlplane
        - external
        - internal_api
        - storage
        - storage_mgmt
      EnabledServices: {get_attr: [ControllerServiceNames, value]}
      ServiceNetMap: {get_attr: [ServiceNetMap, service_net_map_lower]}
      ServiceHostnameList: {get_attr: [Controller, hostname]}
      NetworkHostnameMap: {get_attr: [ControllerNetworkHostnameMap, value]}

  ControllerNetworkHostnameMap:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        # Note (shardy) this somewhat complex yaql may be replaced
        # with a map_deep_merge function in ocata.  It merges the
        # list of maps, but appends to colliding lists so we can
        # create a map of lists for all nodes for each network
        yaql:
          expression: dict($.data.where($ != null).flatten().selectMany($.items()).groupBy($[0], $[1]).select([$[0], $[1].flatten()]))
          data:
            - {get_attr: [Controller, hostname_map]}

  # Combine the NodeAdminUserData and NodeUserData mime archives
  ControllerUserData:
    type: OS::Heat::MultipartMime
    properties:
      parts:
      - config: {get_resource: NodeAdminUserData}
        type: multipart
      - config: {get_resource: NodeTimesyncUserData}
        type: multipart
      - config: {get_resource: NodeUserData}
        type: multipart
      - config: {get_resource: ControllerRoleUserData}
        type: multipart

  # For optional operator role-specific userdata
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  ControllerRoleUserData:
    type: OS::TripleO::Controller::NodeUserData

  Controller:
    type: OS::Heat::ResourceGroup
    depends_on:
      - Networks
      - OVNMacAddressNetwork
    update_policy:
      batch_create:
        max_batch_size: {get_param: NodeCreateBatchSize}
    properties:
      count: {get_param: ControllerCount}
      removal_policies: {get_param: ControllerRemovalPolicies}
      removal_policies_mode: {get_param: ControllerRemovalPoliciesMode}
      resource_def:
        type: OS::TripleO::Controller
        properties:
          CloudDomain: {get_param: CloudDomain}
          ServiceNetMap:
            map_merge:
              - {get_attr: [ServiceNetMap, service_net_map]}
              - {get_param: ControllerServiceNetMap}
          EndpointMap: {get_attr: [EndpointMapData, value]}
          Hostname:
            str_replace:
              template: {get_param: ControllerHostnameFormat}
              params:
                '%stackname%': {get_param: 'OS::stack_name'}
          NodeIndex: '%index%'
          # Note, SchedulerHints must be defined here, not only in the
          # nested template, as it can contain %index%
          ControllerSchedulerHints:
            map_merge:
              - {get_param: ControllerSchedulerHints}
          ServiceNames: {get_attr: [ControllerServiceNames, value]}
          ServiceMetadataSettings: {get_attr: [ControllerServiceChainRoleData, value, service_metadata_settings]}
          OVNBridgeMappings: {get_attr: [ControllerServiceChainRoleData, value, config_settings, 'ovn::controller::ovn_bridge_mappings']}
          DeploymentServerBlacklistDict: {get_attr: [DeploymentServerBlacklistDict, value]}
          RoleParameters:
            map_merge:
              - {}
              - get_param: ControllerParameters
          UserData: {get_resource: ControllerUserData}
  # Resources generated for Compute Role
  ComputeServiceChain:
    type: OS::TripleO::ComputeServices
    properties:
      Services:
        get_param: ComputeServices
      ServiceNetMap:
        map_merge:
          - {get_attr: [ServiceNetMap, service_net_map]}
          - {get_param: ComputeServiceNetMap}
      ServiceData:
        net_cidr_map: {get_attr: [NetCidrMapValue, value]}
        net_vip_map: {get_attr: [VipMap, net_ip_map]}
      EndpointMap: {get_attr: [EndpointMapData, value]}
      DefaultPasswords: {get_attr: [DefaultPasswords, passwords]}
      RoleName: Compute
      RoleParameters:
        map_merge:
          - {'TunedProfileName': 'virtual-host'}
          - get_param: ComputeParameters

  # Lookup of role_data via heat outputs is slow, so workaround this by caching
  # the value in an OS::Heat::Value resource
  ComputeServiceChainRoleData:
    type: OS::Heat::Value
    properties:
      type: json
      value: {get_attr: [ComputeServiceChain, role_data]}

  ComputeNetworkDeploymentActionsValue:
    type: OS::Heat::Value
    properties:
      value:
        if:
        - Compute_network_deployment_actions_exists
        - {get_param: ComputeNetworkDeploymentActions}
        - {get_param: NetworkDeploymentActions}

  ComputeConfigData:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        service_configs: {get_attr: [ComputeServiceConfigSettings, value]}
        service_names: {get_attr: [ComputeServiceNames, value]}
        role_extraconfig:
          map_merge:
            - tripleo::profile::base::metrics::collectd::sensubility::subscriptions: {get_attr: [ComputeServiceChainRoleData, value, monitoring_subscriptions]}
            - {get_param: NovaComputeExtraConfig}
            - {get_param: ComputeExtraConfig}
        extraconfig: {get_param: ExtraConfig}
        hieradata_files:
          - '%{::uuid}'
          - fqdn
          - docker_puppet # Optionally provided by container-puppet.py
          - ansible_managed
          - heat_config_%{::deploy_config_name}
          - config_step
          - role_extraconfig
          - extraconfig
          - pci_passthrough_whitelist
          - service_names
          - service_configs
          - cloud_domain
          - bootstrap_node # provided by tripleo-hieradata
          - all_nodes # provided by tripleo-hieradata
          - vip_data # provided by tripleo-hieradata
          - net_ip_map
          - '%{::osfamily}'
          # The following are required for compatibility with the Controller role
          # where some vendor integrations added hieradata via ExtraConfigPre
          - neutron_bigswitch_data # Optionally provided by Controller/ComputeExtraConfigPre
          # Special variable for upgrade
          - upgrade

  ComputeServiceConfigSettings:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:
          - get_param: GlobalConfigExtraMapData
          - get_attr: [ComputeServiceChainRoleData, value, config_settings]
          
          - get_attr: [ControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailSriovServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailDpdkServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeNFVServiceChainRoleData, value, global_config_settings]
          
          # This next step combines two yaql passes:
          # - The inner one does a deep merge on the service_config_settings for all roles
          # - The outer one filters the map based on the services enabled for the role
          #   then merges the result into one map.
          - yaql:
              expression: let(root => $) -> $.data.map.items().where($[0] in coalesce($root.data.services, [])).select($[1]).reduce($1.mergeWith($2), {})
              data:
                map:
                  yaql:
                    expression: $.data.where($ != null).reduce($1.mergeWith($2), {})
                    data:
                    
                      - get_attr: [ControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailSriovServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailDpdkServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeNFVServiceChainRoleData, value, service_config_settings]
                    
                services: {get_attr: [ComputeServiceNames, value]}

  # Filter any null/None service_names which may be present due to mapping
  # of services to OS::Heat::None
  ComputeServiceNames:
    type: OS::Heat::Value
    depends_on: ComputeServiceChain
    properties:
      type: comma_delimited_list
      value:
        yaql:
          expression: let(root => $) -> distinct($.data.extra_services.items().where($[0] in coalesce($root.data.enabled_services, [])).select($[1]).flatten() + coalesce($root.data.enabled_services, []))
          data:
            enabled_services: {get_attr: [ComputeServiceChainRoleData, value, service_names]}
            extra_services:
              # If anything other than keystone needs this
              # then we should add an extra_networks interface
              # to the service templates role_data but for
              # now we hard-code the keystone special case
              keystone:
                - keystone_admin_api
                - keystone_public_api

  ComputeIpListMap:
    type: OS::TripleO::Network::Ports::NetIpListMap
    properties:
      ControlPlaneIpList: {get_attr: [Compute, ip_address]}
      ExternalIpList: {get_attr: [Compute, external_ip_address]}
      InternalApiIpList: {get_attr: [Compute, internal_api_ip_address]}
      StorageIpList: {get_attr: [Compute, storage_ip_address]}
      StorageMgmtIpList: {get_attr: [Compute, storage_mgmt_ip_address]}
      TenantIpList: {get_attr: [Compute, tenant_ip_address]}
      RoleNetworks:
        - ctlplane
        - external
        - internal_api
        - storage
        - storage_mgmt
        - tenant
      EnabledServices: {get_attr: [ComputeServiceNames, value]}
      ServiceNetMap: {get_attr: [ServiceNetMap, service_net_map_lower]}
      ServiceHostnameList: {get_attr: [Compute, hostname]}
      NetworkHostnameMap: {get_attr: [ComputeNetworkHostnameMap, value]}

  ComputeNetworkHostnameMap:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        # Note (shardy) this somewhat complex yaql may be replaced
        # with a map_deep_merge function in ocata.  It merges the
        # list of maps, but appends to colliding lists so we can
        # create a map of lists for all nodes for each network
        yaql:
          expression: dict($.data.where($ != null).flatten().selectMany($.items()).groupBy($[0], $[1]).select([$[0], $[1].flatten()]))
          data:
            - {get_attr: [Compute, hostname_map]}

  # Combine the NodeAdminUserData and NodeUserData mime archives
  ComputeUserData:
    type: OS::Heat::MultipartMime
    properties:
      parts:
      - config: {get_resource: NodeAdminUserData}
        type: multipart
      - config: {get_resource: NodeTimesyncUserData}
        type: multipart
      - config: {get_resource: NodeUserData}
        type: multipart
      - config: {get_resource: ComputeRoleUserData}
        type: multipart

  # For optional operator role-specific userdata
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  ComputeRoleUserData:
    type: OS::TripleO::Compute::NodeUserData

  Compute:
    type: OS::Heat::ResourceGroup
    depends_on:
      - Networks
      - OVNMacAddressNetwork
    update_policy:
      batch_create:
        max_batch_size: {get_param: NodeCreateBatchSize}
    properties:
      count: {get_param: ComputeCount}
      removal_policies: {get_param: ComputeRemovalPolicies}
      removal_policies_mode: {get_param: ComputeRemovalPoliciesMode}
      resource_def:
        type: OS::TripleO::Compute
        properties:
          CloudDomain: {get_param: CloudDomain}
          ServiceNetMap:
            map_merge:
              - {get_attr: [ServiceNetMap, service_net_map]}
              - {get_param: ComputeServiceNetMap}
          EndpointMap: {get_attr: [EndpointMapData, value]}
          Hostname:
            str_replace:
              template: {get_param: ComputeHostnameFormat}
              params:
                '%stackname%': {get_param: 'OS::stack_name'}
          NodeIndex: '%index%'
          # Note, SchedulerHints must be defined here, not only in the
          # nested template, as it can contain %index%
          ComputeSchedulerHints:
            map_merge:
              - {get_param: NovaComputeSchedulerHints}
              - {get_param: ComputeSchedulerHints}
          ServiceNames: {get_attr: [ComputeServiceNames, value]}
          ServiceMetadataSettings: {get_attr: [ComputeServiceChainRoleData, value, service_metadata_settings]}
          OVNBridgeMappings: {get_attr: [ComputeServiceChainRoleData, value, config_settings, 'ovn::controller::ovn_bridge_mappings']}
          DeploymentServerBlacklistDict: {get_attr: [DeploymentServerBlacklistDict, value]}
          RoleParameters:
            map_merge:
              - {'TunedProfileName': 'virtual-host'}
              - get_param: ComputeParameters
          UserData: {get_resource: ComputeUserData}
  # Resources generated for ContrailController Role
  ContrailControllerServiceChain:
    type: OS::TripleO::ContrailControllerServices
    properties:
      Services:
        get_param: ContrailControllerServices
      ServiceNetMap:
        map_merge:
          - {get_attr: [ServiceNetMap, service_net_map]}
          - {get_param: ContrailControllerServiceNetMap}
      ServiceData:
        net_cidr_map: {get_attr: [NetCidrMapValue, value]}
        net_vip_map: {get_attr: [VipMap, net_ip_map]}
      EndpointMap: {get_attr: [EndpointMapData, value]}
      DefaultPasswords: {get_attr: [DefaultPasswords, passwords]}
      RoleName: ContrailController
      RoleParameters:
        map_merge:
          - {}
          - get_param: ContrailControllerParameters

  # Lookup of role_data via heat outputs is slow, so workaround this by caching
  # the value in an OS::Heat::Value resource
  ContrailControllerServiceChainRoleData:
    type: OS::Heat::Value
    properties:
      type: json
      value: {get_attr: [ContrailControllerServiceChain, role_data]}

  ContrailControllerNetworkDeploymentActionsValue:
    type: OS::Heat::Value
    properties:
      value:
        if:
        - ContrailController_network_deployment_actions_exists
        - {get_param: ContrailControllerNetworkDeploymentActions}
        - {get_param: NetworkDeploymentActions}

  ContrailControllerConfigData:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        service_configs: {get_attr: [ContrailControllerServiceConfigSettings, value]}
        service_names: {get_attr: [ContrailControllerServiceNames, value]}
        role_extraconfig:
          map_merge:
            - tripleo::profile::base::metrics::collectd::sensubility::subscriptions: {get_attr: [ContrailControllerServiceChainRoleData, value, monitoring_subscriptions]}
            - {get_param: ContrailControllerExtraConfig}
        extraconfig: {get_param: ExtraConfig}
        hieradata_files:
          - '%{::uuid}'
          - fqdn
          - docker_puppet # Optionally provided by container-puppet.py
          - ansible_managed
          - heat_config_%{::deploy_config_name}
          - config_step
          - role_extraconfig
          - extraconfig
          - pci_passthrough_whitelist
          - service_names
          - service_configs
          - cloud_domain
          - bootstrap_node # provided by tripleo-hieradata
          - all_nodes # provided by tripleo-hieradata
          - vip_data # provided by tripleo-hieradata
          - net_ip_map
          - '%{::osfamily}'
          # The following are required for compatibility with the Controller role
          # where some vendor integrations added hieradata via ExtraConfigPre
          - neutron_bigswitch_data # Optionally provided by Controller/ComputeExtraConfigPre
          # Special variable for upgrade
          - upgrade

  ContrailControllerServiceConfigSettings:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:
          - get_param: GlobalConfigExtraMapData
          - get_attr: [ContrailControllerServiceChainRoleData, value, config_settings]
          
          - get_attr: [ControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailSriovServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailDpdkServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeNFVServiceChainRoleData, value, global_config_settings]
          
          # This next step combines two yaql passes:
          # - The inner one does a deep merge on the service_config_settings for all roles
          # - The outer one filters the map based on the services enabled for the role
          #   then merges the result into one map.
          - yaql:
              expression: let(root => $) -> $.data.map.items().where($[0] in coalesce($root.data.services, [])).select($[1]).reduce($1.mergeWith($2), {})
              data:
                map:
                  yaql:
                    expression: $.data.where($ != null).reduce($1.mergeWith($2), {})
                    data:
                    
                      - get_attr: [ControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailSriovServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailDpdkServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeNFVServiceChainRoleData, value, service_config_settings]
                    
                services: {get_attr: [ContrailControllerServiceNames, value]}

  # Filter any null/None service_names which may be present due to mapping
  # of services to OS::Heat::None
  ContrailControllerServiceNames:
    type: OS::Heat::Value
    depends_on: ContrailControllerServiceChain
    properties:
      type: comma_delimited_list
      value:
        yaql:
          expression: let(root => $) -> distinct($.data.extra_services.items().where($[0] in coalesce($root.data.enabled_services, [])).select($[1]).flatten() + coalesce($root.data.enabled_services, []))
          data:
            enabled_services: {get_attr: [ContrailControllerServiceChainRoleData, value, service_names]}
            extra_services:
              # If anything other than keystone needs this
              # then we should add an extra_networks interface
              # to the service templates role_data but for
              # now we hard-code the keystone special case
              keystone:
                - keystone_admin_api
                - keystone_public_api

  ContrailControllerIpListMap:
    type: OS::TripleO::Network::Ports::NetIpListMap
    properties:
      ControlPlaneIpList: {get_attr: [ContrailController, ip_address]}
      ExternalIpList: {get_attr: [ContrailController, external_ip_address]}
      InternalApiIpList: {get_attr: [ContrailController, internal_api_ip_address]}
      TenantIpList: {get_attr: [ContrailController, tenant_ip_address]}
      RoleNetworks:
        - ctlplane
        - external
        - internal_api
        - tenant
      EnabledServices: {get_attr: [ContrailControllerServiceNames, value]}
      ServiceNetMap: {get_attr: [ServiceNetMap, service_net_map_lower]}
      ServiceHostnameList: {get_attr: [ContrailController, hostname]}
      NetworkHostnameMap: {get_attr: [ContrailControllerNetworkHostnameMap, value]}

  ContrailControllerNetworkHostnameMap:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        # Note (shardy) this somewhat complex yaql may be replaced
        # with a map_deep_merge function in ocata.  It merges the
        # list of maps, but appends to colliding lists so we can
        # create a map of lists for all nodes for each network
        yaql:
          expression: dict($.data.where($ != null).flatten().selectMany($.items()).groupBy($[0], $[1]).select([$[0], $[1].flatten()]))
          data:
            - {get_attr: [ContrailController, hostname_map]}

  # Combine the NodeAdminUserData and NodeUserData mime archives
  ContrailControllerUserData:
    type: OS::Heat::MultipartMime
    properties:
      parts:
      - config: {get_resource: NodeAdminUserData}
        type: multipart
      - config: {get_resource: NodeTimesyncUserData}
        type: multipart
      - config: {get_resource: NodeUserData}
        type: multipart
      - config: {get_resource: ContrailControllerRoleUserData}
        type: multipart

  # For optional operator role-specific userdata
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  ContrailControllerRoleUserData:
    type: OS::TripleO::ContrailController::NodeUserData

  ContrailController:
    type: OS::Heat::ResourceGroup
    depends_on:
      - Networks
      - OVNMacAddressNetwork
    update_policy:
      batch_create:
        max_batch_size: {get_param: NodeCreateBatchSize}
    properties:
      count: {get_param: ContrailControllerCount}
      removal_policies: {get_param: ContrailControllerRemovalPolicies}
      removal_policies_mode: {get_param: ContrailControllerRemovalPoliciesMode}
      resource_def:
        type: OS::TripleO::ContrailController
        properties:
          CloudDomain: {get_param: CloudDomain}
          ServiceNetMap:
            map_merge:
              - {get_attr: [ServiceNetMap, service_net_map]}
              - {get_param: ContrailControllerServiceNetMap}
          EndpointMap: {get_attr: [EndpointMapData, value]}
          Hostname:
            str_replace:
              template: {get_param: ContrailControllerHostnameFormat}
              params:
                '%stackname%': {get_param: 'OS::stack_name'}
          NodeIndex: '%index%'
          # Note, SchedulerHints must be defined here, not only in the
          # nested template, as it can contain %index%
          ContrailControllerSchedulerHints:
            map_merge:
              - {get_param: ContrailControllerSchedulerHints}
          ServiceNames: {get_attr: [ContrailControllerServiceNames, value]}
          ServiceMetadataSettings: {get_attr: [ContrailControllerServiceChainRoleData, value, service_metadata_settings]}
          OVNBridgeMappings: {get_attr: [ContrailControllerServiceChainRoleData, value, config_settings, 'ovn::controller::ovn_bridge_mappings']}
          DeploymentServerBlacklistDict: {get_attr: [DeploymentServerBlacklistDict, value]}
          RoleParameters:
            map_merge:
              - {}
              - get_param: ContrailControllerParameters
          UserData: {get_resource: ContrailControllerUserData}
  # Resources generated for ContrailSriov Role
  ContrailSriovServiceChain:
    type: OS::TripleO::ContrailSriovServices
    properties:
      Services:
        get_param: ContrailSriovServices
      ServiceNetMap:
        map_merge:
          - {get_attr: [ServiceNetMap, service_net_map]}
          - {get_param: ContrailSriovServiceNetMap}
      ServiceData:
        net_cidr_map: {get_attr: [NetCidrMapValue, value]}
        net_vip_map: {get_attr: [VipMap, net_ip_map]}
      EndpointMap: {get_attr: [EndpointMapData, value]}
      DefaultPasswords: {get_attr: [DefaultPasswords, passwords]}
      RoleName: ContrailSriov
      RoleParameters:
        map_merge:
          - {'TunedProfileName': 'cpu-partitioning'}
          - get_param: ContrailSriovParameters

  # Lookup of role_data via heat outputs is slow, so workaround this by caching
  # the value in an OS::Heat::Value resource
  ContrailSriovServiceChainRoleData:
    type: OS::Heat::Value
    properties:
      type: json
      value: {get_attr: [ContrailSriovServiceChain, role_data]}

  ContrailSriovNetworkDeploymentActionsValue:
    type: OS::Heat::Value
    properties:
      value:
        if:
        - ContrailSriov_network_deployment_actions_exists
        - {get_param: ContrailSriovNetworkDeploymentActions}
        - {get_param: NetworkDeploymentActions}

  ContrailSriovConfigData:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        service_configs: {get_attr: [ContrailSriovServiceConfigSettings, value]}
        service_names: {get_attr: [ContrailSriovServiceNames, value]}
        role_extraconfig:
          map_merge:
            - tripleo::profile::base::metrics::collectd::sensubility::subscriptions: {get_attr: [ContrailSriovServiceChainRoleData, value, monitoring_subscriptions]}
            - {get_param: ContrailSriovExtraConfig}
        extraconfig: {get_param: ExtraConfig}
        hieradata_files:
          - '%{::uuid}'
          - fqdn
          - docker_puppet # Optionally provided by container-puppet.py
          - ansible_managed
          - heat_config_%{::deploy_config_name}
          - config_step
          - role_extraconfig
          - extraconfig
          - pci_passthrough_whitelist
          - service_names
          - service_configs
          - cloud_domain
          - bootstrap_node # provided by tripleo-hieradata
          - all_nodes # provided by tripleo-hieradata
          - vip_data # provided by tripleo-hieradata
          - net_ip_map
          - '%{::osfamily}'
          # The following are required for compatibility with the Controller role
          # where some vendor integrations added hieradata via ExtraConfigPre
          - neutron_bigswitch_data # Optionally provided by Controller/ComputeExtraConfigPre
          # Special variable for upgrade
          - upgrade

  ContrailSriovServiceConfigSettings:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:
          - get_param: GlobalConfigExtraMapData
          - get_attr: [ContrailSriovServiceChainRoleData, value, config_settings]
          
          - get_attr: [ControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailSriovServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailDpdkServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeNFVServiceChainRoleData, value, global_config_settings]
          
          # This next step combines two yaql passes:
          # - The inner one does a deep merge on the service_config_settings for all roles
          # - The outer one filters the map based on the services enabled for the role
          #   then merges the result into one map.
          - yaql:
              expression: let(root => $) -> $.data.map.items().where($[0] in coalesce($root.data.services, [])).select($[1]).reduce($1.mergeWith($2), {})
              data:
                map:
                  yaql:
                    expression: $.data.where($ != null).reduce($1.mergeWith($2), {})
                    data:
                    
                      - get_attr: [ControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailSriovServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailDpdkServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeNFVServiceChainRoleData, value, service_config_settings]
                    
                services: {get_attr: [ContrailSriovServiceNames, value]}

  # Filter any null/None service_names which may be present due to mapping
  # of services to OS::Heat::None
  ContrailSriovServiceNames:
    type: OS::Heat::Value
    depends_on: ContrailSriovServiceChain
    properties:
      type: comma_delimited_list
      value:
        yaql:
          expression: let(root => $) -> distinct($.data.extra_services.items().where($[0] in coalesce($root.data.enabled_services, [])).select($[1]).flatten() + coalesce($root.data.enabled_services, []))
          data:
            enabled_services: {get_attr: [ContrailSriovServiceChainRoleData, value, service_names]}
            extra_services:
              # If anything other than keystone needs this
              # then we should add an extra_networks interface
              # to the service templates role_data but for
              # now we hard-code the keystone special case
              keystone:
                - keystone_admin_api
                - keystone_public_api

  ContrailSriovIpListMap:
    type: OS::TripleO::Network::Ports::NetIpListMap
    properties:
      ControlPlaneIpList: {get_attr: [ContrailSriov, ip_address]}
      ExternalIpList: {get_attr: [ContrailSriov, external_ip_address]}
      InternalApiIpList: {get_attr: [ContrailSriov, internal_api_ip_address]}
      StorageIpList: {get_attr: [ContrailSriov, storage_ip_address]}
      StorageMgmtIpList: {get_attr: [ContrailSriov, storage_mgmt_ip_address]}
      TenantIpList: {get_attr: [ContrailSriov, tenant_ip_address]}
      RoleNetworks:
        - ctlplane
        - external
        - internal_api
        - storage
        - storage_mgmt
        - tenant
      EnabledServices: {get_attr: [ContrailSriovServiceNames, value]}
      ServiceNetMap: {get_attr: [ServiceNetMap, service_net_map_lower]}
      ServiceHostnameList: {get_attr: [ContrailSriov, hostname]}
      NetworkHostnameMap: {get_attr: [ContrailSriovNetworkHostnameMap, value]}

  ContrailSriovNetworkHostnameMap:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        # Note (shardy) this somewhat complex yaql may be replaced
        # with a map_deep_merge function in ocata.  It merges the
        # list of maps, but appends to colliding lists so we can
        # create a map of lists for all nodes for each network
        yaql:
          expression: dict($.data.where($ != null).flatten().selectMany($.items()).groupBy($[0], $[1]).select([$[0], $[1].flatten()]))
          data:
            - {get_attr: [ContrailSriov, hostname_map]}

  # Combine the NodeAdminUserData and NodeUserData mime archives
  ContrailSriovUserData:
    type: OS::Heat::MultipartMime
    properties:
      parts:
      - config: {get_resource: NodeAdminUserData}
        type: multipart
      - config: {get_resource: NodeTimesyncUserData}
        type: multipart
      - config: {get_resource: NodeUserData}
        type: multipart
      - config: {get_resource: ContrailSriovRoleUserData}
        type: multipart

  # For optional operator role-specific userdata
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  ContrailSriovRoleUserData:
    type: OS::TripleO::ContrailSriov::NodeUserData

  ContrailSriov:
    type: OS::Heat::ResourceGroup
    depends_on:
      - Networks
      - OVNMacAddressNetwork
    update_policy:
      batch_create:
        max_batch_size: {get_param: NodeCreateBatchSize}
    properties:
      count: {get_param: ContrailSriovCount}
      removal_policies: {get_param: ContrailSriovRemovalPolicies}
      removal_policies_mode: {get_param: ContrailSriovRemovalPoliciesMode}
      resource_def:
        type: OS::TripleO::ContrailSriov
        properties:
          CloudDomain: {get_param: CloudDomain}
          ServiceNetMap:
            map_merge:
              - {get_attr: [ServiceNetMap, service_net_map]}
              - {get_param: ContrailSriovServiceNetMap}
          EndpointMap: {get_attr: [EndpointMapData, value]}
          Hostname:
            str_replace:
              template: {get_param: ContrailSriovHostnameFormat}
              params:
                '%stackname%': {get_param: 'OS::stack_name'}
          NodeIndex: '%index%'
          # Note, SchedulerHints must be defined here, not only in the
          # nested template, as it can contain %index%
          ContrailSriovSchedulerHints:
            map_merge:
              - {get_param: ContrailSriovSchedulerHints}
          ServiceNames: {get_attr: [ContrailSriovServiceNames, value]}
          ServiceMetadataSettings: {get_attr: [ContrailSriovServiceChainRoleData, value, service_metadata_settings]}
          OVNBridgeMappings: {get_attr: [ContrailSriovServiceChainRoleData, value, config_settings, 'ovn::controller::ovn_bridge_mappings']}
          DeploymentServerBlacklistDict: {get_attr: [DeploymentServerBlacklistDict, value]}
          RoleParameters:
            map_merge:
              - {'TunedProfileName': 'cpu-partitioning'}
              - get_param: ContrailSriovParameters
          UserData: {get_resource: ContrailSriovUserData}
  # Resources generated for ContrailDpdk Role
  ContrailDpdkServiceChain:
    type: OS::TripleO::ContrailDpdkServices
    properties:
      Services:
        get_param: ContrailDpdkServices
      ServiceNetMap:
        map_merge:
          - {get_attr: [ServiceNetMap, service_net_map]}
          - {get_param: ContrailDpdkServiceNetMap}
      ServiceData:
        net_cidr_map: {get_attr: [NetCidrMapValue, value]}
        net_vip_map: {get_attr: [VipMap, net_ip_map]}
      EndpointMap: {get_attr: [EndpointMapData, value]}
      DefaultPasswords: {get_attr: [DefaultPasswords, passwords]}
      RoleName: ContrailDpdk
      RoleParameters:
        map_merge:
          - {'TunedProfileName': 'cpu-partitioning'}
          - get_param: ContrailDpdkParameters

  # Lookup of role_data via heat outputs is slow, so workaround this by caching
  # the value in an OS::Heat::Value resource
  ContrailDpdkServiceChainRoleData:
    type: OS::Heat::Value
    properties:
      type: json
      value: {get_attr: [ContrailDpdkServiceChain, role_data]}

  ContrailDpdkNetworkDeploymentActionsValue:
    type: OS::Heat::Value
    properties:
      value:
        if:
        - ContrailDpdk_network_deployment_actions_exists
        - {get_param: ContrailDpdkNetworkDeploymentActions}
        - {get_param: NetworkDeploymentActions}

  ContrailDpdkConfigData:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        service_configs: {get_attr: [ContrailDpdkServiceConfigSettings, value]}
        service_names: {get_attr: [ContrailDpdkServiceNames, value]}
        role_extraconfig:
          map_merge:
            - tripleo::profile::base::metrics::collectd::sensubility::subscriptions: {get_attr: [ContrailDpdkServiceChainRoleData, value, monitoring_subscriptions]}
            - {get_param: ContrailDpdkExtraConfig}
        extraconfig: {get_param: ExtraConfig}
        hieradata_files:
          - '%{::uuid}'
          - fqdn
          - docker_puppet # Optionally provided by container-puppet.py
          - ansible_managed
          - heat_config_%{::deploy_config_name}
          - config_step
          - role_extraconfig
          - extraconfig
          - pci_passthrough_whitelist
          - service_names
          - service_configs
          - cloud_domain
          - bootstrap_node # provided by tripleo-hieradata
          - all_nodes # provided by tripleo-hieradata
          - vip_data # provided by tripleo-hieradata
          - net_ip_map
          - '%{::osfamily}'
          # The following are required for compatibility with the Controller role
          # where some vendor integrations added hieradata via ExtraConfigPre
          - neutron_bigswitch_data # Optionally provided by Controller/ComputeExtraConfigPre
          # Special variable for upgrade
          - upgrade

  ContrailDpdkServiceConfigSettings:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:
          - get_param: GlobalConfigExtraMapData
          - get_attr: [ContrailDpdkServiceChainRoleData, value, config_settings]
          
          - get_attr: [ControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailSriovServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailDpdkServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeNFVServiceChainRoleData, value, global_config_settings]
          
          # This next step combines two yaql passes:
          # - The inner one does a deep merge on the service_config_settings for all roles
          # - The outer one filters the map based on the services enabled for the role
          #   then merges the result into one map.
          - yaql:
              expression: let(root => $) -> $.data.map.items().where($[0] in coalesce($root.data.services, [])).select($[1]).reduce($1.mergeWith($2), {})
              data:
                map:
                  yaql:
                    expression: $.data.where($ != null).reduce($1.mergeWith($2), {})
                    data:
                    
                      - get_attr: [ControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailSriovServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailDpdkServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeNFVServiceChainRoleData, value, service_config_settings]
                    
                services: {get_attr: [ContrailDpdkServiceNames, value]}

  # Filter any null/None service_names which may be present due to mapping
  # of services to OS::Heat::None
  ContrailDpdkServiceNames:
    type: OS::Heat::Value
    depends_on: ContrailDpdkServiceChain
    properties:
      type: comma_delimited_list
      value:
        yaql:
          expression: let(root => $) -> distinct($.data.extra_services.items().where($[0] in coalesce($root.data.enabled_services, [])).select($[1]).flatten() + coalesce($root.data.enabled_services, []))
          data:
            enabled_services: {get_attr: [ContrailDpdkServiceChainRoleData, value, service_names]}
            extra_services:
              # If anything other than keystone needs this
              # then we should add an extra_networks interface
              # to the service templates role_data but for
              # now we hard-code the keystone special case
              keystone:
                - keystone_admin_api
                - keystone_public_api

  ContrailDpdkIpListMap:
    type: OS::TripleO::Network::Ports::NetIpListMap
    properties:
      ControlPlaneIpList: {get_attr: [ContrailDpdk, ip_address]}
      ExternalIpList: {get_attr: [ContrailDpdk, external_ip_address]}
      InternalApiIpList: {get_attr: [ContrailDpdk, internal_api_ip_address]}
      StorageIpList: {get_attr: [ContrailDpdk, storage_ip_address]}
      StorageMgmtIpList: {get_attr: [ContrailDpdk, storage_mgmt_ip_address]}
      TenantIpList: {get_attr: [ContrailDpdk, tenant_ip_address]}
      RoleNetworks:
        - ctlplane
        - external
        - internal_api
        - storage
        - storage_mgmt
        - tenant
      EnabledServices: {get_attr: [ContrailDpdkServiceNames, value]}
      ServiceNetMap: {get_attr: [ServiceNetMap, service_net_map_lower]}
      ServiceHostnameList: {get_attr: [ContrailDpdk, hostname]}
      NetworkHostnameMap: {get_attr: [ContrailDpdkNetworkHostnameMap, value]}

  ContrailDpdkNetworkHostnameMap:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        # Note (shardy) this somewhat complex yaql may be replaced
        # with a map_deep_merge function in ocata.  It merges the
        # list of maps, but appends to colliding lists so we can
        # create a map of lists for all nodes for each network
        yaql:
          expression: dict($.data.where($ != null).flatten().selectMany($.items()).groupBy($[0], $[1]).select([$[0], $[1].flatten()]))
          data:
            - {get_attr: [ContrailDpdk, hostname_map]}

  # Combine the NodeAdminUserData and NodeUserData mime archives
  ContrailDpdkUserData:
    type: OS::Heat::MultipartMime
    properties:
      parts:
      - config: {get_resource: NodeAdminUserData}
        type: multipart
      - config: {get_resource: NodeTimesyncUserData}
        type: multipart
      - config: {get_resource: NodeUserData}
        type: multipart
      - config: {get_resource: ContrailDpdkRoleUserData}
        type: multipart

  # For optional operator role-specific userdata
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  ContrailDpdkRoleUserData:
    type: OS::TripleO::ContrailDpdk::NodeUserData

  ContrailDpdk:
    type: OS::Heat::ResourceGroup
    depends_on:
      - Networks
      - OVNMacAddressNetwork
    update_policy:
      batch_create:
        max_batch_size: {get_param: NodeCreateBatchSize}
    properties:
      count: {get_param: ContrailDpdkCount}
      removal_policies: {get_param: ContrailDpdkRemovalPolicies}
      removal_policies_mode: {get_param: ContrailDpdkRemovalPoliciesMode}
      resource_def:
        type: OS::TripleO::ContrailDpdk
        properties:
          CloudDomain: {get_param: CloudDomain}
          ServiceNetMap:
            map_merge:
              - {get_attr: [ServiceNetMap, service_net_map]}
              - {get_param: ContrailDpdkServiceNetMap}
          EndpointMap: {get_attr: [EndpointMapData, value]}
          Hostname:
            str_replace:
              template: {get_param: ContrailDpdkHostnameFormat}
              params:
                '%stackname%': {get_param: 'OS::stack_name'}
          NodeIndex: '%index%'
          # Note, SchedulerHints must be defined here, not only in the
          # nested template, as it can contain %index%
          ContrailDpdkSchedulerHints:
            map_merge:
              - {get_param: ContrailDpdkSchedulerHints}
          ServiceNames: {get_attr: [ContrailDpdkServiceNames, value]}
          ServiceMetadataSettings: {get_attr: [ContrailDpdkServiceChainRoleData, value, service_metadata_settings]}
          OVNBridgeMappings: {get_attr: [ContrailDpdkServiceChainRoleData, value, config_settings, 'ovn::controller::ovn_bridge_mappings']}
          DeploymentServerBlacklistDict: {get_attr: [DeploymentServerBlacklistDict, value]}
          RoleParameters:
            map_merge:
              - {'TunedProfileName': 'cpu-partitioning'}
              - get_param: ContrailDpdkParameters
          UserData: {get_resource: ContrailDpdkUserData}
  # Resources generated for ComputeNFV Role
  ComputeNFVServiceChain:
    type: OS::TripleO::ComputeNFVServices
    properties:
      Services:
        get_param: ComputeNFVServices
      ServiceNetMap:
        map_merge:
          - {get_attr: [ServiceNetMap, service_net_map]}
          - {get_param: ComputeNFVServiceNetMap}
      ServiceData:
        net_cidr_map: {get_attr: [NetCidrMapValue, value]}
        net_vip_map: {get_attr: [VipMap, net_ip_map]}
      EndpointMap: {get_attr: [EndpointMapData, value]}
      DefaultPasswords: {get_attr: [DefaultPasswords, passwords]}
      RoleName: ComputeNFV
      RoleParameters:
        map_merge:
          - {'TunedProfileName': 'cpu-partitioning'}
          - get_param: ComputeNFVParameters

  # Lookup of role_data via heat outputs is slow, so workaround this by caching
  # the value in an OS::Heat::Value resource
  ComputeNFVServiceChainRoleData:
    type: OS::Heat::Value
    properties:
      type: json
      value: {get_attr: [ComputeNFVServiceChain, role_data]}

  ComputeNFVNetworkDeploymentActionsValue:
    type: OS::Heat::Value
    properties:
      value:
        if:
        - ComputeNFV_network_deployment_actions_exists
        - {get_param: ComputeNFVNetworkDeploymentActions}
        - {get_param: NetworkDeploymentActions}

  ComputeNFVConfigData:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        service_configs: {get_attr: [ComputeNFVServiceConfigSettings, value]}
        service_names: {get_attr: [ComputeNFVServiceNames, value]}
        role_extraconfig:
          map_merge:
            - tripleo::profile::base::metrics::collectd::sensubility::subscriptions: {get_attr: [ComputeNFVServiceChainRoleData, value, monitoring_subscriptions]}
            - {get_param: ComputeNFVExtraConfig}
        extraconfig: {get_param: ExtraConfig}
        hieradata_files:
          - '%{::uuid}'
          - fqdn
          - docker_puppet # Optionally provided by container-puppet.py
          - ansible_managed
          - heat_config_%{::deploy_config_name}
          - config_step
          - role_extraconfig
          - extraconfig
          - pci_passthrough_whitelist
          - service_names
          - service_configs
          - cloud_domain
          - bootstrap_node # provided by tripleo-hieradata
          - all_nodes # provided by tripleo-hieradata
          - vip_data # provided by tripleo-hieradata
          - net_ip_map
          - '%{::osfamily}'
          # The following are required for compatibility with the Controller role
          # where some vendor integrations added hieradata via ExtraConfigPre
          - neutron_bigswitch_data # Optionally provided by Controller/ComputeExtraConfigPre
          # Special variable for upgrade
          - upgrade

  ComputeNFVServiceConfigSettings:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:
          - get_param: GlobalConfigExtraMapData
          - get_attr: [ComputeNFVServiceChainRoleData, value, config_settings]
          
          - get_attr: [ControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailControllerServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailSriovServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ContrailDpdkServiceChainRoleData, value, global_config_settings]
          
          - get_attr: [ComputeNFVServiceChainRoleData, value, global_config_settings]
          
          # This next step combines two yaql passes:
          # - The inner one does a deep merge on the service_config_settings for all roles
          # - The outer one filters the map based on the services enabled for the role
          #   then merges the result into one map.
          - yaql:
              expression: let(root => $) -> $.data.map.items().where($[0] in coalesce($root.data.services, [])).select($[1]).reduce($1.mergeWith($2), {})
              data:
                map:
                  yaql:
                    expression: $.data.where($ != null).reduce($1.mergeWith($2), {})
                    data:
                    
                      - get_attr: [ControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailControllerServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailSriovServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ContrailDpdkServiceChainRoleData, value, service_config_settings]
                    
                      - get_attr: [ComputeNFVServiceChainRoleData, value, service_config_settings]
                    
                services: {get_attr: [ComputeNFVServiceNames, value]}

  # Filter any null/None service_names which may be present due to mapping
  # of services to OS::Heat::None
  ComputeNFVServiceNames:
    type: OS::Heat::Value
    depends_on: ComputeNFVServiceChain
    properties:
      type: comma_delimited_list
      value:
        yaql:
          expression: let(root => $) -> distinct($.data.extra_services.items().where($[0] in coalesce($root.data.enabled_services, [])).select($[1]).flatten() + coalesce($root.data.enabled_services, []))
          data:
            enabled_services: {get_attr: [ComputeNFVServiceChainRoleData, value, service_names]}
            extra_services:
              # If anything other than keystone needs this
              # then we should add an extra_networks interface
              # to the service templates role_data but for
              # now we hard-code the keystone special case
              keystone:
                - keystone_admin_api
                - keystone_public_api

  ComputeNFVIpListMap:
    type: OS::TripleO::Network::Ports::NetIpListMap
    properties:
      ControlPlaneIpList: {get_attr: [ComputeNFV, ip_address]}
      ExternalIpList: {get_attr: [ComputeNFV, external_ip_address]}
      InternalApiIpList: {get_attr: [ComputeNFV, internal_api_ip_address]}
      StorageIpList: {get_attr: [ComputeNFV, storage_ip_address]}
      StorageMgmtIpList: {get_attr: [ComputeNFV, storage_mgmt_ip_address]}
      TenantIpList: {get_attr: [ComputeNFV, tenant_ip_address]}
      RoleNetworks:
        - ctlplane
        - external
        - internal_api
        - storage
        - storage_mgmt
        - tenant
      EnabledServices: {get_attr: [ComputeNFVServiceNames, value]}
      ServiceNetMap: {get_attr: [ServiceNetMap, service_net_map_lower]}
      ServiceHostnameList: {get_attr: [ComputeNFV, hostname]}
      NetworkHostnameMap: {get_attr: [ComputeNFVNetworkHostnameMap, value]}

  ComputeNFVNetworkHostnameMap:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        # Note (shardy) this somewhat complex yaql may be replaced
        # with a map_deep_merge function in ocata.  It merges the
        # list of maps, but appends to colliding lists so we can
        # create a map of lists for all nodes for each network
        yaql:
          expression: dict($.data.where($ != null).flatten().selectMany($.items()).groupBy($[0], $[1]).select([$[0], $[1].flatten()]))
          data:
            - {get_attr: [ComputeNFV, hostname_map]}

  # Combine the NodeAdminUserData and NodeUserData mime archives
  ComputeNFVUserData:
    type: OS::Heat::MultipartMime
    properties:
      parts:
      - config: {get_resource: NodeAdminUserData}
        type: multipart
      - config: {get_resource: NodeTimesyncUserData}
        type: multipart
      - config: {get_resource: NodeUserData}
        type: multipart
      - config: {get_resource: ComputeNFVRoleUserData}
        type: multipart

  # For optional operator role-specific userdata
  # Should return a OS::Heat::MultipartMime reference via OS::stack_id
  ComputeNFVRoleUserData:
    type: OS::TripleO::ComputeNFV::NodeUserData

  ComputeNFV:
    type: OS::Heat::ResourceGroup
    depends_on:
      - Networks
      - OVNMacAddressNetwork
    update_policy:
      batch_create:
        max_batch_size: {get_param: NodeCreateBatchSize}
    properties:
      count: {get_param: ComputeNFVCount}
      removal_policies: {get_param: ComputeNFVRemovalPolicies}
      removal_policies_mode: {get_param: ComputeNFVRemovalPoliciesMode}
      resource_def:
        type: OS::TripleO::ComputeNFV
        properties:
          CloudDomain: {get_param: CloudDomain}
          ServiceNetMap:
            map_merge:
              - {get_attr: [ServiceNetMap, service_net_map]}
              - {get_param: ComputeNFVServiceNetMap}
          EndpointMap: {get_attr: [EndpointMapData, value]}
          Hostname:
            str_replace:
              template: {get_param: ComputeNFVHostnameFormat}
              params:
                '%stackname%': {get_param: 'OS::stack_name'}
          NodeIndex: '%index%'
          # Note, SchedulerHints must be defined here, not only in the
          # nested template, as it can contain %index%
          ComputeNFVSchedulerHints:
            map_merge:
              - {get_param: ComputeNFVSchedulerHints}
          ServiceNames: {get_attr: [ComputeNFVServiceNames, value]}
          ServiceMetadataSettings: {get_attr: [ComputeNFVServiceChainRoleData, value, service_metadata_settings]}
          OVNBridgeMappings: {get_attr: [ComputeNFVServiceChainRoleData, value, config_settings, 'ovn::controller::ovn_bridge_mappings']}
          DeploymentServerBlacklistDict: {get_attr: [DeploymentServerBlacklistDict, value]}
          RoleParameters:
            map_merge:
              - {'TunedProfileName': 'cpu-partitioning'}
              - get_param: ComputeNFVParameters
          UserData: {get_resource: ComputeNFVUserData}
  ControllerServers:
    type: OS::Heat::Value
    depends_on: Controller
    properties:
      type: json
      value:
        yaql:
          expression: let(servers=>switch(isDict($.data.servers) => $.data.servers, true => {})) -> $servers.deleteAll($servers.keys().where($servers[$] = null))
          data:
            servers: {get_attr: [Controller, attributes, nova_server_resource]}
  ComputeServers:
    type: OS::Heat::Value
    depends_on: Compute
    properties:
      type: json
      value:
        yaql:
          expression: let(servers=>switch(isDict($.data.servers) => $.data.servers, true => {})) -> $servers.deleteAll($servers.keys().where($servers[$] = null))
          data:
            servers: {get_attr: [Compute, attributes, nova_server_resource]}
  ContrailControllerServers:
    type: OS::Heat::Value
    depends_on: ContrailController
    properties:
      type: json
      value:
        yaql:
          expression: let(servers=>switch(isDict($.data.servers) => $.data.servers, true => {})) -> $servers.deleteAll($servers.keys().where($servers[$] = null))
          data:
            servers: {get_attr: [ContrailController, attributes, nova_server_resource]}
  ContrailSriovServers:
    type: OS::Heat::Value
    depends_on: ContrailSriov
    properties:
      type: json
      value:
        yaql:
          expression: let(servers=>switch(isDict($.data.servers) => $.data.servers, true => {})) -> $servers.deleteAll($servers.keys().where($servers[$] = null))
          data:
            servers: {get_attr: [ContrailSriov, attributes, nova_server_resource]}
  ContrailDpdkServers:
    type: OS::Heat::Value
    depends_on: ContrailDpdk
    properties:
      type: json
      value:
        yaql:
          expression: let(servers=>switch(isDict($.data.servers) => $.data.servers, true => {})) -> $servers.deleteAll($servers.keys().where($servers[$] = null))
          data:
            servers: {get_attr: [ContrailDpdk, attributes, nova_server_resource]}
  ComputeNFVServers:
    type: OS::Heat::Value
    depends_on: ComputeNFV
    properties:
      type: json
      value:
        yaql:
          expression: let(servers=>switch(isDict($.data.servers) => $.data.servers, true => {})) -> $servers.deleteAll($servers.keys().where($servers[$] = null))
          data:
            servers: {get_attr: [ComputeNFV, attributes, nova_server_resource]}

  # This is a different format to *Servers, as it creates a map of lists
  # whereas *Servers creates a map of maps with keys of the nested resource names
  ServerIdMap:
    type: OS::Heat::Value
    properties:
      value:
        server_ids:
          Controller: {get_attr: [Controller, nova_server_resource]}
          Compute: {get_attr: [Compute, nova_server_resource]}
          ContrailController: {get_attr: [ContrailController, nova_server_resource]}
          ContrailSriov: {get_attr: [ContrailSriov, nova_server_resource]}
          ContrailDpdk: {get_attr: [ContrailDpdk, nova_server_resource]}
          ComputeNFV: {get_attr: [ComputeNFV, nova_server_resource]}
        bootstrap_server_id:
          yaql:
            expression: coalesce($.data, []).first(null)
            data: {get_attr: [Controller, nova_server_resource]}

  # This resource just creates a dict out of the DeploymentServerBlacklist,
  # which is a list. The dict is used in the role templates to set a condition
  # on whether to create the deployment resources. We can't use the list
  # directly because there is no way to ask Heat if a list contains a specific
  # value.
  DeploymentServerBlacklistDict:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:
          repeat:
            template:
              hostname: 1
            for_each:
              hostname: {get_param: DeploymentServerBlacklist}

  HostsEntryValue:
    type: OS::Heat::Value
    properties:
      type: comma_delimited_list
      value:
        list_concat_unique:
          list_concat:
            - - {get_param: UndercloudHostsEntries}
            - - if:
                - add_vips_to_etc_hosts
                - {get_attr: [VipHosts, value]}
                - []
            - {get_attr: [Controller, hosts_entry]}
            - {get_attr: [Compute, hosts_entry]}
            - {get_attr: [ContrailController, hosts_entry]}
            - {get_attr: [ContrailSriov, hosts_entry]}
            - {get_attr: [ContrailDpdk, hosts_entry]}
            - {get_attr: [ComputeNFV, hosts_entry]}
            - - {get_param: ExtraHostFileEntries}

  CloudNames:
    type: OS::Heat::Value
    properties:
      value:
        # Special case the External hostname param, which is CloudName
        cloud_name_external: {get_param: CloudName}
        # Special case the Internal API hostname param, which is CloudNameInternal
        cloud_name_internal_api: {get_param: CloudNameInternal}
        cloud_name_storage: {get_param: CloudNameStorage}
        # Special case StorageMgmt hostname param, which is CloudNameStorageManagement
        cloud_name_storage_mgmt: {get_param: CloudNameStorageManagement}
        cloud_name_ctlplane: {get_param: CloudNameCtlplane}

  GlobalConfig:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        map_merge:

          - get_attr: [ControllerServiceChainRoleData, value, global_config_settings]

          - get_attr: [ComputeServiceChainRoleData, value, global_config_settings]

          - get_attr: [ContrailControllerServiceChainRoleData, value, global_config_settings]

          - get_attr: [ContrailSriovServiceChainRoleData, value, global_config_settings]

          - get_attr: [ContrailDpdkServiceChainRoleData, value, global_config_settings]

          - get_attr: [ComputeNFVServiceChainRoleData, value, global_config_settings]


  MysqlRootPassword:
    type: OS::TripleO::RandomString
    properties:
      length: 10

  RabbitCookie:
    type: OS::TripleO::RandomString
    properties:
      length: 20
      salt: {get_param: RabbitCookieSalt}

  DefaultPasswords:
    type: OS::TripleO::DefaultPasswords
    properties:
      DefaultMysqlRootPassword: {get_attr: [MysqlRootPassword, value]}
      DefaultRabbitCookie: {get_attr: [RabbitCookie, value]}
      DefaultHeatAuthEncryptionKey: {get_attr: [HeatAuthEncryptionKey, value]}
      DefaultPcsdPassword: {get_attr: [PcsdPassword, value]}
      DefaultHorizonSecret: {get_attr: [HorizonSecret, value]}

  # creates the network architecture
  Networks:
    type: OS::TripleO::Network
    properties:
      CtlplaneNetworkCidrs: {get_attr: [ControlVirtualIP, network, tags]}

  OVNMacAddressNetwork:
    type: OS::TripleO::OVNMacAddressNetwork
  ControllerGroupVars:
    type: OS::Heat::Value
    properties:
      value:
        role_networks:
          - External
        
        
          - InternalApi
        
        
          - Storage
        
        
          - StorageMgmt
        
        
        
        service_metadata_settings: {get_attr: [ControllerServiceChainRoleData, value, service_metadata_settings]}


  ComputeGroupVars:
    type: OS::Heat::Value
    properties:
      value:
        role_networks:
          - External
        
        
          - InternalApi
        
        
          - Storage
        
        
          - StorageMgmt
        
        
          - Tenant
        
        
        service_metadata_settings: {get_attr: [ComputeServiceChainRoleData, value, service_metadata_settings]}


  ContrailControllerGroupVars:
    type: OS::Heat::Value
    properties:
      value:
        role_networks:
          - External
        
        
          - InternalApi
        
        
        
        
          - Tenant
        
        
        service_metadata_settings: {get_attr: [ContrailControllerServiceChainRoleData, value, service_metadata_settings]}


  ContrailSriovGroupVars:
    type: OS::Heat::Value
    properties:
      value:
        role_networks:
          - External
        
        
          - InternalApi
        
        
          - Storage
        
        
          - StorageMgmt
        
        
          - Tenant
        
        
        service_metadata_settings: {get_attr: [ContrailSriovServiceChainRoleData, value, service_metadata_settings]}


  ContrailDpdkGroupVars:
    type: OS::Heat::Value
    properties:
      value:
        role_networks:
          - External
        
        
          - InternalApi
        
        
          - Storage
        
        
          - StorageMgmt
        
        
          - Tenant
        
        
        service_metadata_settings: {get_attr: [ContrailDpdkServiceChainRoleData, value, service_metadata_settings]}


  ComputeNFVGroupVars:
    type: OS::Heat::Value
    properties:
      value:
        role_networks:
          - External
        
        
          - InternalApi
        
        
          - Storage
        
        
          - StorageMgmt
        
        
          - Tenant
        
        
        service_metadata_settings: {get_attr: [ComputeNFVServiceChainRoleData, value, service_metadata_settings]}



  ControlVirtualIP:
    depends_on: ServiceNetMap
    type: OS::TripleO::Network::Ports::ControlPlaneVipPort
    properties:
      name: control_virtual_ip
      network: {get_param: NeutronControlPlaneID}
      fixed_ips:
        if:
        - control_fixed_ip_not_set
        - [{subnet: {get_attr: [ServiceNetMap, vip_subnet_map, ctlplane]}}]
        - get_param: ControlFixedIPs
      replacement_policy: AUTO
      tags:
        - tripleo_vip_net=ctlplane
        - str_replace:
            template: tripleo_stack_name=$STACK_NAME
            params:
              $STACK_NAME: {get_param: 'OS::stack_name'}

  RedisVirtualIP:
    depends_on: [Networks, ServiceNetMap]
    type: OS::TripleO::Network::Ports::RedisVipPort
    properties:
      ControlPlaneIP: {get_attr: [ControlVirtualIP, fixed_ips, 0, ip_address]}
      ControlPlaneSubnetCidr:
        if:
          - ctlplane_subnet_cidr_set
          - {get_param: ControlPlaneSubnetCidr}
          - {str_split: ['/', {get_attr: [ControlVirtualIP, subnets, 0, cidr]}, 1]}
      ControlPlaneNetwork: {get_param: NeutronControlPlaneID}
      PortName: redis_virtual_ip
      NetworkName: {get_attr: [ServiceNetMap, service_net_map, RedisNetwork]}
      ServiceName: redis
      FixedIPs:
        if:
        - redis_virtual_fixed_ip_set
        - {get_param: RedisVirtualFixedIPs}
        - [{subnet: {get_attr: [ServiceNetMap, vip_subnet_map, redis]}}]

  OVNDBsVirtualIP:
    depends_on: [Networks, ServiceNetMap]
    type: OS::TripleO::Network::Ports::OVNDBsVipPort
    properties:
      ControlPlaneIP: {get_attr: [ControlVirtualIP, fixed_ips, 0, ip_address]}
      ControlPlaneSubnetCidr:
        if:
          - ctlplane_subnet_cidr_set
          - {get_param: ControlPlaneSubnetCidr}
          - {str_split: ['/', {get_attr: [ControlVirtualIP, subnets, 0, cidr]}, 1]}
      ControlPlaneNetwork: {get_param: NeutronControlPlaneID}
      PortName: ovn_dbs_virtual_ip
      NetworkName: {get_attr: [ServiceNetMap, service_net_map, OvnDbsNetwork]}
      ServiceName: ovn_dbs
      FixedIPs:
        if:
        - ovn_dbs_virtual_fixed_ip_set
        - {get_param: OVNDBsVirtualFixedIPs}
        - [{subnet: {get_attr: [ServiceNetMap, vip_subnet_map, ovn_dbs]}}]
  # The public VIP is on the External net, falls back to ctlplane
  PublicVirtualIP:
    depends_on: [Networks, ServiceNetMap]
    type: OS::TripleO::Network::Ports::ExternalVipPort
    properties:
      ControlPlaneIP: {get_attr: [ControlVirtualIP, fixed_ips, 0, ip_address]}
      ControlPlaneSubnetCidr:
        if:
          - ctlplane_subnet_cidr_set
          - {get_param: ControlPlaneSubnetCidr}
          - {str_split: ['/', {get_attr: [ControlVirtualIP, subnets, 0, cidr]}, 1]}
      ControlPlaneNetwork: {get_param: NeutronControlPlaneID}
      PortName: public_virtual_ip
      FixedIPs:
        if:
        - public_virtual_fixed_ip_set
        - {get_param: PublicVirtualFixedIPs}
        - [{subnet: {get_attr: [ServiceNetMap, vip_subnet_map, External]}}]
  InternalApiVirtualIP:
    depends_on: [Networks, ServiceNetMap]
    type: OS::TripleO::Network::Ports::InternalApiVipPort
    properties:
      ControlPlaneIP: {get_attr: [ControlVirtualIP, fixed_ips, 0, ip_address]}
      ControlPlaneSubnetCidr:
        if:
          - ctlplane_subnet_cidr_set
          - {get_param: ControlPlaneSubnetCidr}
          - {str_split: ['/', {get_attr: [ControlVirtualIP, subnets, 0, cidr]}, 1]}
      PortName: internal_api_virtual_ip
      FixedIPs:
        if:
        - internal_api_virtual_fixed_ip_set
        - {get_param: InternalApiVirtualFixedIPs}
        - [{subnet: {get_attr: [ServiceNetMap, vip_subnet_map, InternalApi]}}]
      IsVirtualIP: true
  
  StorageVirtualIP:
    depends_on: [Networks, ServiceNetMap]
    type: OS::TripleO::Network::Ports::StorageVipPort
    properties:
      ControlPlaneIP: {get_attr: [ControlVirtualIP, fixed_ips, 0, ip_address]}
      ControlPlaneSubnetCidr:
        if:
          - ctlplane_subnet_cidr_set
          - {get_param: ControlPlaneSubnetCidr}
          - {str_split: ['/', {get_attr: [ControlVirtualIP, subnets, 0, cidr]}, 1]}
      PortName: storage_virtual_ip
      FixedIPs:
        if:
        - storage_virtual_fixed_ip_set
        - {get_param: StorageVirtualFixedIPs}
        - [{subnet: {get_attr: [ServiceNetMap, vip_subnet_map, Storage]}}]
      IsVirtualIP: true
  
  StorageMgmtVirtualIP:
    depends_on: [Networks, ServiceNetMap]
    type: OS::TripleO::Network::Ports::StorageMgmtVipPort
    properties:
      ControlPlaneIP: {get_attr: [ControlVirtualIP, fixed_ips, 0, ip_address]}
      ControlPlaneSubnetCidr:
        if:
          - ctlplane_subnet_cidr_set
          - {get_param: ControlPlaneSubnetCidr}
          - {str_split: ['/', {get_attr: [ControlVirtualIP, subnets, 0, cidr]}, 1]}
      PortName: storage_mgmt_virtual_ip
      FixedIPs:
        if:
        - storage_mgmt_virtual_fixed_ip_set
        - {get_param: StorageMgmtVirtualFixedIPs}
        - [{subnet: {get_attr: [ServiceNetMap, vip_subnet_map, StorageMgmt]}}]
      IsVirtualIP: true
  

  VipMap:
    type: OS::TripleO::Network::Ports::NetVipMap
    properties:
      ControlPlaneIp: {get_attr: [ControlVirtualIP, fixed_ips, 0, ip_address]}
      ControlPlaneSubnetCidr:
        if:
          - ctlplane_subnet_cidr_set
          - {get_param: ControlPlaneSubnetCidr}
          - {str_split: ['/', {get_attr: [ControlVirtualIP, subnets, 0, cidr]}, 1]}
      ExternalIp: {get_attr: [PublicVirtualIP, ip_address]}
      ExternalIpUri: {get_attr: [PublicVirtualIP, ip_address_uri]}
      InternalApiIp: {get_attr: [InternalApiVirtualIP, ip_address]}
      InternalApiIpUri: {get_attr: [InternalApiVirtualIP, ip_address_uri]}
      StorageIp: {get_attr: [StorageVirtualIP, ip_address]}
      StorageIpUri: {get_attr: [StorageVirtualIP, ip_address_uri]}
      StorageMgmtIp: {get_attr: [StorageMgmtVirtualIP, ip_address]}
      StorageMgmtIpUri: {get_attr: [StorageMgmtVirtualIP, ip_address_uri]}
      # No tenant or management VIP required
    # Because of nested get_attr functions in the KeystoneAdminVip output, we
    # can't determine which attributes of VipMap are used until after
    # ServiceNetMap's attribute values are available.
    depends_on: ServiceNetMap

  # Optional ExtraConfig for all nodes - all roles are passed in here, but
  # the nested template may configure each role differently (or not at all)
  AllNodesExtraConfig:
    type: OS::TripleO::AllNodesExtraConfig
    properties:
      servers:
        Controller: {get_attr: [ControllerServers, value]}
        Compute: {get_attr: [ComputeServers, value]}
        ContrailController: {get_attr: [ContrailControllerServers, value]}
        ContrailSriov: {get_attr: [ContrailSriovServers, value]}
        ContrailDpdk: {get_attr: [ContrailDpdkServers, value]}
        ComputeNFV: {get_attr: [ComputeNFVServers, value]}

  BlacklistedIpAddresses:
    type: OS::Heat::Value
    properties:
      value:
        list_concat:
          - {get_attr: [Controller, blacklist_ip_address]}
          - {get_attr: [Compute, blacklist_ip_address]}
          - {get_attr: [ContrailController, blacklist_ip_address]}
          - {get_attr: [ContrailSriov, blacklist_ip_address]}
          - {get_attr: [ContrailDpdk, blacklist_ip_address]}
          - {get_attr: [ComputeNFV, blacklist_ip_address]}

  AnsibleHostVars:
    type: OS::Heat::Value
    properties:
      type: json
      value:
        Controller:
          map_merge:
            list_concat:
            - {get_attr: [Controller, ansible_host_vars_map]}
        Compute:
          map_merge:
            list_concat:
            - {get_attr: [Compute, ansible_host_vars_map]}
        ContrailController:
          map_merge:
            list_concat:
            - {get_attr: [ContrailController, ansible_host_vars_map]}
        ContrailSriov:
          map_merge:
            list_concat:
            - {get_attr: [ContrailSriov, ansible_host_vars_map]}
        ContrailDpdk:
          map_merge:
            list_concat:
            - {get_attr: [ContrailDpdk, ansible_host_vars_map]}
        ComputeNFV:
          map_merge:
            list_concat:
            - {get_attr: [ComputeNFV, ansible_host_vars_map]}

  BlacklistedHostnames:
    type: OS::Heat::Value
    properties:
      value:
        list_concat:
          - {get_attr: [Controller, blacklist_hostname]}
          - {get_attr: [Compute, blacklist_hostname]}
          - {get_attr: [ContrailController, blacklist_hostname]}
          - {get_attr: [ContrailSriov, blacklist_hostname]}
          - {get_attr: [ContrailDpdk, blacklist_hostname]}
          - {get_attr: [ComputeNFV, blacklist_hostname]}

  # Post deployment steps for all roles
  AllNodesDeploySteps:
    type: OS::TripleO::PostDeploySteps
    depends_on:
      - AllNodesExtraConfig
    properties:
      servers:
        Controller: {get_attr: [ControllerServers, value]}
        Compute: {get_attr: [ComputeServers, value]}
        ContrailController: {get_attr: [ContrailControllerServers, value]}
        ContrailSriov: {get_attr: [ContrailSriovServers, value]}
        ContrailDpdk: {get_attr: [ContrailDpdkServers, value]}
        ComputeNFV: {get_attr: [ComputeNFVServers, value]}
      EndpointMap: {get_attr: [EndpointMapData, value]}
      role_data:
        Controller: {get_attr: [ControllerServiceChainRoleData, value]}
        Compute: {get_attr: [ComputeServiceChainRoleData, value]}
        ContrailController: {get_attr: [ContrailControllerServiceChainRoleData, value]}
        ContrailSriov: {get_attr: [ContrailSriovServiceChainRoleData, value]}
        ContrailDpdk: {get_attr: [ContrailDpdkServiceChainRoleData, value]}
        ComputeNFV: {get_attr: [ComputeNFVServiceChainRoleData, value]}
      ControllerCount: {get_param: ControllerCount}
      ComputeCount: {get_param: ComputeCount}
      ContrailControllerCount: {get_param: ContrailControllerCount}
      ContrailSriovCount: {get_param: ContrailSriovCount}
      ContrailDpdkCount: {get_param: ContrailDpdkCount}
      ComputeNFVCount: {get_param: ComputeNFVCount}
      ServiceNetMapLower: {get_attr: [ServiceNetMap, service_net_map_lower]}
      PingTestIpsMap:
        Controller:
          list_join:
          - ' '
          - - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Controller, ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Controller, external_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Controller, internal_api_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Controller, storage_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Controller, storage_mgmt_ip_address]}
        Compute:
          list_join:
          - ' '
          - - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Compute, ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Compute, external_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Compute, internal_api_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Compute, storage_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Compute, storage_mgmt_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [Compute, tenant_ip_address]}
        ContrailController:
          list_join:
          - ' '
          - - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailController, ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailController, external_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailController, internal_api_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailController, tenant_ip_address]}
        ContrailSriov:
          list_join:
          - ' '
          - - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailSriov, ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailSriov, external_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailSriov, internal_api_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailSriov, storage_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailSriov, storage_mgmt_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailSriov, tenant_ip_address]}
        ContrailDpdk:
          list_join:
          - ' '
          - - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailDpdk, ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailDpdk, external_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailDpdk, internal_api_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailDpdk, storage_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailDpdk, storage_mgmt_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ContrailDpdk, tenant_ip_address]}
        ComputeNFV:
          list_join:
          - ' '
          - - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ComputeNFV, ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ComputeNFV, external_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ComputeNFV, internal_api_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ComputeNFV, storage_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ComputeNFV, storage_mgmt_ip_address]}
            - yaql:
                expression: coalesce($.data, []).first(null)
                data: {get_attr: [ComputeNFV, tenant_ip_address]}
      HostsEntry: {get_attr: [HostsEntryValue, value]}
      EnabledServices:
        list_concat:
          - {get_attr: [ControllerServiceNames, value]}
          - {get_attr: [ComputeServiceNames, value]}
          - {get_attr: [ContrailControllerServiceNames, value]}
          - {get_attr: [ContrailSriovServiceNames, value]}
          - {get_attr: [ContrailDpdkServiceNames, value]}
          - {get_attr: [ComputeNFVServiceNames, value]}
      ControlVirtualIP: {get_attr: [ControlVirtualIP, fixed_ips, 0, ip_address]}
      EnabledNetworks:
        - External
        - InternalApi
        - Storage
        - StorageMgmt
        - Tenant
      NetVipMap:
        map_merge:
          - {get_attr: [VipMap, net_ip_map]}
          - redis: {get_attr: [RedisVirtualIP, ip_address]}
          - ovn_dbs: {get_attr: [OVNDBsVirtualIP, ip_address]}
      CloudNames: {get_attr: [CloudNames, value]}
      UndercloudHostsEntries: {get_param: UndercloudHostsEntries}
      ExtraHostsEntries: {get_param: ExtraHostFileEntries}
      VipHostsEntries:
        if:
          - add_vips_to_etc_hosts
          - {get_attr: [VipHosts, value]}
          - []
      KeystoneResourcesConfigs:
        map_merge:

          - get_attr: [ControllerServiceChainRoleData, value, keystone_resources]

          - get_attr: [ComputeServiceChainRoleData, value, keystone_resources]

          - get_attr: [ContrailControllerServiceChainRoleData, value, keystone_resources]

          - get_attr: [ContrailSriovServiceChainRoleData, value, keystone_resources]

          - get_attr: [ContrailDpdkServiceChainRoleData, value, keystone_resources]

          - get_attr: [ComputeNFVServiceChainRoleData, value, keystone_resources]


outputs:
  ManagedEndpoints:
    description: Asserts that the keystone endpoints have been provisioned.
    value: true
  KeystoneURL:
    description: URL for the Overcloud Keystone service
    value: {get_attr: [EndpointMapData, value, KeystonePublic, uri_no_suffix]}
  KeystoneAdminVip:
    description: Keystone Admin VIP endpoint
    # Note that these nested get_attr functions require a dependency
    # relationship between VipMap and ServiceNetMap, since we can't determine
    # which attributes of VipMap are used until after ServiceNetMap's attribute
    # values are available. If this is ever reworked to not use nested
    # get_attr, that dependency can be removed.
    value: {get_attr: [VipMap, net_ip_map, {get_attr: [ServiceNetMap, service_net_map, KeystoneAdminApiNetwork]}]}
  EndpointMap:
    description: |
      Mapping of the resources with the needed info for their endpoints.
      This includes the protocol used, the IP, port and also a full
      representation of the URI.
    value: {get_attr: [EndpointMapData, value]}
  HostsEntry:
    description: |
      The content that should be appended to your /etc/hosts if you want to get
      hostname-based access to the deployed nodes (useful for testing without
      setting up a DNS).
    value:
      list_concat_unique:
        - {get_attr: [HostsEntryValue, value]}
        - {get_attr: [VipHosts, value]}
  EnabledServices:
    description: The services enabled on each role
    value:
      Controller: {get_attr: [ControllerServiceNames, value]}
      Compute: {get_attr: [ComputeServiceNames, value]}
      ContrailController: {get_attr: [ContrailControllerServiceNames, value]}
      ContrailSriov: {get_attr: [ContrailSriovServiceNames, value]}
      ContrailDpdk: {get_attr: [ContrailDpdkServiceNames, value]}
      ComputeNFV: {get_attr: [ComputeNFVServiceNames, value]}
  RoleData:
    description: The configuration data associated with each role
    value:
      Controller: {get_attr: [ControllerServiceChainRoleData, value]}
      Compute: {get_attr: [ComputeServiceChainRoleData, value]}
      ContrailController: {get_attr: [ContrailControllerServiceChainRoleData, value]}
      ContrailSriov: {get_attr: [ContrailSriovServiceChainRoleData, value]}
      ContrailDpdk: {get_attr: [ContrailDpdkServiceChainRoleData, value]}
      ComputeNFV: {get_attr: [ComputeNFVServiceChainRoleData, value]}
  RoleConfig:
    description: The configuration workflows associated with each role
    value: {get_attr: [AllNodesDeploySteps, RoleConfig]}
  RoleNetIpMap:
    description: Mapping of each network to a list of IPs for each role
    value:
      Controller: {get_attr: [ControllerIpListMap, net_ip_map]}
      Compute: {get_attr: [ComputeIpListMap, net_ip_map]}
      ContrailController: {get_attr: [ContrailControllerIpListMap, net_ip_map]}
      ContrailSriov: {get_attr: [ContrailSriovIpListMap, net_ip_map]}
      ContrailDpdk: {get_attr: [ContrailDpdkIpListMap, net_ip_map]}
      ComputeNFV: {get_attr: [ComputeNFVIpListMap, net_ip_map]}
  RoleGroupVars:
    description: Mapping of roles to ansible group_vars to be applied config in those roles
    value:
      Controller:
        map_merge:
        - {get_attr: [ControllerGroupVars, value]}
        - {get_attr: [ControllerConfigData, value]}
        - any_errors_fatal: {get_param: ControllerAnyErrorsFatal}
          max_fail_percentage: {get_param: ControllerMaxFailPercentage}
          neutron_physical_bridge_name: {get_param: NeutronPhysicalBridge}
          neutron_public_interface_name: {get_param: NeutronPublicInterface}
          network_deployment_actions: {get_attr: [ControllerNetworkDeploymentActionsValue, value]}
          tripleo_stack_name: {get_param: RootStackName}
        - {get_param: ControllerExtraGroupVars}
      Compute:
        map_merge:
        - {get_attr: [ComputeGroupVars, value]}
        - {get_attr: [ComputeConfigData, value]}
        - any_errors_fatal: {get_param: ComputeAnyErrorsFatal}
          max_fail_percentage: {get_param: ComputeMaxFailPercentage}
          neutron_physical_bridge_name: {get_param: NeutronPhysicalBridge}
          neutron_public_interface_name: {get_param: NeutronPublicInterface}
          network_deployment_actions: {get_attr: [ComputeNetworkDeploymentActionsValue, value]}
          tripleo_stack_name: {get_param: RootStackName}
        - {get_param: ComputeExtraGroupVars}
      ContrailController:
        map_merge:
        - {get_attr: [ContrailControllerGroupVars, value]}
        - {get_attr: [ContrailControllerConfigData, value]}
        - any_errors_fatal: {get_param: ContrailControllerAnyErrorsFatal}
          max_fail_percentage: {get_param: ContrailControllerMaxFailPercentage}
          neutron_physical_bridge_name: {get_param: NeutronPhysicalBridge}
          neutron_public_interface_name: {get_param: NeutronPublicInterface}
          network_deployment_actions: {get_attr: [ContrailControllerNetworkDeploymentActionsValue, value]}
          tripleo_stack_name: {get_param: RootStackName}
        - {get_param: ContrailControllerExtraGroupVars}
      ContrailSriov:
        map_merge:
        - {get_attr: [ContrailSriovGroupVars, value]}
        - {get_attr: [ContrailSriovConfigData, value]}
        - any_errors_fatal: {get_param: ContrailSriovAnyErrorsFatal}
          max_fail_percentage: {get_param: ContrailSriovMaxFailPercentage}
          neutron_physical_bridge_name: {get_param: NeutronPhysicalBridge}
          neutron_public_interface_name: {get_param: NeutronPublicInterface}
          network_deployment_actions: {get_attr: [ContrailSriovNetworkDeploymentActionsValue, value]}
          tripleo_stack_name: {get_param: RootStackName}
        - {get_param: ContrailSriovExtraGroupVars}
      ContrailDpdk:
        map_merge:
        - {get_attr: [ContrailDpdkGroupVars, value]}
        - {get_attr: [ContrailDpdkConfigData, value]}
        - any_errors_fatal: {get_param: ContrailDpdkAnyErrorsFatal}
          max_fail_percentage: {get_param: ContrailDpdkMaxFailPercentage}
          neutron_physical_bridge_name: {get_param: NeutronPhysicalBridge}
          neutron_public_interface_name: {get_param: NeutronPublicInterface}
          network_deployment_actions: {get_attr: [ContrailDpdkNetworkDeploymentActionsValue, value]}
          tripleo_stack_name: {get_param: RootStackName}
        - {get_param: ContrailDpdkExtraGroupVars}
      ComputeNFV:
        map_merge:
        - {get_attr: [ComputeNFVGroupVars, value]}
        - {get_attr: [ComputeNFVConfigData, value]}
        - any_errors_fatal: {get_param: ComputeNFVAnyErrorsFatal}
          max_fail_percentage: {get_param: ComputeNFVMaxFailPercentage}
          neutron_physical_bridge_name: {get_param: NeutronPhysicalBridge}
          neutron_public_interface_name: {get_param: NeutronPublicInterface}
          network_deployment_actions: {get_attr: [ComputeNFVNetworkDeploymentActionsValue, value]}
          tripleo_stack_name: {get_param: RootStackName}
        - {get_param: ComputeNFVExtraGroupVars}
  RoleNetHostnameMap:
    description: Mapping of each network to a list of hostnames for each role
    value:
      Controller: {get_attr: [ControllerNetworkHostnameMap, value]}
      Compute: {get_attr: [ComputeNetworkHostnameMap, value]}
      ContrailController: {get_attr: [ContrailControllerNetworkHostnameMap, value]}
      ContrailSriov: {get_attr: [ContrailSriovNetworkHostnameMap, value]}
      ContrailDpdk: {get_attr: [ContrailDpdkNetworkHostnameMap, value]}
      ComputeNFV: {get_attr: [ComputeNFVNetworkHostnameMap, value]}
  RoleTags:
    description: Tags for each role, as defined in roles_data.yaml
    value:
      Controller: ['primary', 'controller', 'external_bridge']
      Compute: ['external_bridge']
      ContrailController: ['primary', 'contrailcontroller']
      ContrailSriov: ['contrailsriov']
      ContrailDpdk: ['contraildpdk']
      ComputeNFV: ['computenfv']
  VipMap:
    description: Mapping of each network to VIP addresses. Also includes the Redis and OVN DBs VIPs.
    value:
      map_merge:
        - {get_attr: [VipMap, net_ip_map]}
        - redis: {get_attr: [RedisVirtualIP, ip_address]}
        - ovn_dbs: {get_attr: [OVNDBsVirtualIP, ip_address]}
  ServerIdData:
    description: Mapping of each role to a list of nova server IDs and the bootstrap ID
    value: {get_attr: [ServerIdMap, value]}
  BlacklistedHostnames:
    description: List of blacklisted hostnames
    value: {get_attr: [BlacklistedHostnames, value]}
  BlacklistedIpAddresses:
    description: List of blacklisted ctlplane IP addresses
    value: {get_attr: [BlacklistedIpAddresses, value]}
  GlobalConfig:
    description: The global_config (hieradata).
    value: {get_attr: [GlobalConfig, value]}
  HostnameNetworkConfigMap:
    description: Mapping of hostname to NetworkConfig resource
    value:
      map_merge:
        list_concat:
          - {get_attr: [Controller, hostname_network_config_map]}
          - {get_attr: [Compute, hostname_network_config_map]}
          - {get_attr: [ContrailController, hostname_network_config_map]}
          - {get_attr: [ContrailSriov, hostname_network_config_map]}
          - {get_attr: [ContrailDpdk, hostname_network_config_map]}
          - {get_attr: [ComputeNFV, hostname_network_config_map]}
  AnsibleHostVarsMap:
    description: Map of Ansible Host variables per role
    value: {get_attr: [AnsibleHostVars, value]}